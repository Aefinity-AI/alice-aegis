# Adversarial review verdicts — 2026-08-01 (verbatim archive)

Every result lane merged today was independently attacked by a reviewer agent that re-ran
tests/evaluators/benches itself before merge. These are the verbatim structured verdicts,
archived from the orchestration journals so 'reviews are on record' is a citable statement.
Reviewer effort/labels per the workflow scripts in this repo's session records.


## Kernel-candidate gauntlet (bitplane / fused / ReLU2-sparsity + cross-reviewer) — `wf_d81263a8-11a`

**bitplane_verdict: SOUND**

**fused_verdict: SOUND**

**sparsity_verdict: SOUND**

> ADVERSARIAL REVIEW — all claims re-verified by running the artifacts, not by reading self-reports.
> 
> === LANE 1: BITPLANE (wt /home/killboxincorporated/.claude/worktrees/wf_d81263a8-11a-1, commit 35b9628) — SOUND ===
> Attacks attempted and results:
> - Bit order in pack_bitplanes: verified against the incumbent's UNPACK_LUT semantics (code at bit 2*lane of byte cp covers column cp*4+lane; plane bit c%64 of word c/64, LE byte j = columns 8j..8j+7 matches the vpbroadcastd/vpshufb/vpand(BITSEL)/vpcmpeqd expansion; ctrl bytes 0..3 < 0x80 so no pshufb zeroing). Round-trip test pins it, including injected 0b11111111 byte degrading to 0 like UNPACK_LUT.
> - Masked-add sign errors / byte-identity: the order-preserving claim holds analytically (fma(x,±1,s)=s±x single rounding; masked +0.0 legs exact because accumulators can't reach -0.0 from +0.0 start) AND empirically — I reran all 5 tests myself: pass in debug, release, and release+parallel; shapes cover remainder rows and packed tail (37x91 = 1 remainder row, 16 vector + 6 tail bytes). Loop bounds and tail expression ((x0*w0+x1*w1)+x2*w2)+x3*w3 are token-identical to ops.rs:501-526/564-572; vector-loop byte reads proven in-bounds (bo+4 <= words*8).
> - Settled negatives: NO trailing_zeros/bit-scan/per-nonzero branch anywhere (grep of full diff; only doc-comment mentions). 2 bits/weight = incumbent decode traffic, so not A7 either; the pshufb here is mask expansion, not 4-bit LUT-mpGEMM.
> - Bench baseline: ternary_matvec dispatch confirmed serial in the default build (parallel path is feature-gated, ops.rs:674-697); pre- AND post-timing byte-identity gates; I smoke-ran it — both gates true, no timing recorded. Incumbent ops.rs has ZERO diff.
> - UEFI claim: aegis-core lib and full aegis-uefi build for x86_64-unknown-uefi verified (exit 0); AVX2 kernels correctly cfg-gated off uefi with the LLVM-legalization bisection documented; pack + both scalar refs remain all-target.
> Nits (non-blocking): (1) if someone builds the bench with --features parallel the incumbent arm goes multi-threaded while candidates stay serial — documented in a comment but not guarded (add a compile_error! or runtime assert before any logged run); (2) rep loop always runs incumbent→(i)→(ii), never order-swaps like lane 2's bench — satisfies the interleaving requirement but is weaker; (3) the pre-existing lib warning (sampler.rs top_p) is not theirs.
> 
> === LANE 2: FUSED (wt ...-11a-2, commit 1315f01) — SOUND ===
> - Incumbent untouched: ops.rs diff is 375 pure additions, zero deletions (verified).
> - Call-pattern claim verified in inference.rs: decode Q/K/V are three ternary_matvec calls on arena.norm_state (lines ~577/585/593), gate/up two calls on norm_state (~670-688).
> - Byte-identity is structural (lut8! is the incumbent unpack token-for-token; per-row FMA chains independent so cross-matrix interleave can't change per-row rounding; delegation offset done=blocks*4 is 4-aligned) and verified: all 6 tests pass in debug, release, release+parallel — including GQA 2560/640/640, unequal dims, <4-row tails, tail-only dim_in 20/36, no-op-on-undersized and no-write-past-dim_out contracts. Bench smoke-run: byte-identical in all 4 scenarios, order-swapped interleave, identity gate aborts timing on divergence.
> - UEFI: I forced a clean recompile (touch ops.rs) of aegis-core for x86_64-unknown-uefi — builds; aegis-uefi builds. The fused FMA form legalizes where lane 1's masked add/sub form crashed LLVM, so no cfg-gate is needed; claim honest.
> - "Full suite green with --test-threads=1": verified exactly as stated (gemm_equivalence 3/3 with -j1). Their qualifier matters — see cross-lane finding below.
> - GPR-spill disassembly note is properly labeled qualitative; no timing recorded anywhere.
> 
> === LANE 3: SPARSITY (wt ...-11a-3, commit 19093d7) — SOUND, decision number independently reproduced bit-for-bit ===
> - Counting the right vector: both counters sit immediately before the down_proj matvec/matmul on the exact buffer it consumes (arena.up decode / arena.batch_up prefill), AFTER relu2|silu gate, up*=gate, optional ffn_sub_norm, and quant_act — i.e. NOT the pre-activation gate and NOT the pre-quant product. Verified in source at both sites.
> - Right configs: BitNet run logged layers=30 hidden=2560 inter=6912 hidden_act=Relu2 (aegis_pruned_config.json carries "relu2"; model.rs defaults Relu2 when absent; ops.rs relu2 zeroes non-positives exactly); M7 control logged hidden_act=Silu, layers=7/384/1024. int8_act confirmed a default feature; INT8_ACT_ENABLED const is truthful.
> - STRONGEST CHECK: I rebuilt the probe and reran BOTH runs on the same artifacts. Every per-layer number, every min/max, token counts (74 prefill/27 decode; 37/128 control) and pooled means (0.7888/0.7786; control 0.2092/0.2139) reproduce EXACTLY. The log (docs/hardware_logs/relu2_act_sparsity_bitnet2b_2026-08-01.log, identical copies in worktree and main tree) is genuine instrument output; repro at scratchpad/{m7_repro,bitnet_repro}.txt.
> - Arithmetic audited: 17,694,720/69,468,160 = 25.5% ✓; 132.7 MB ✓; 17.0% incl. 257.3 MB LM head ✓; 20.1%/13.4% savings ✓; break-even z = eps/(1+eps) ✓ (eps=1 → 0.50; 0.79 → 2.4x headroom ✓); pooled means equal per-layer table averages (awk: 0.78883/0.77859) ✓.
> - Rules: no ledger row (Rule B respected pending verify-figures in main tree); log is a NEW append-only file (Rule C); counts only, machine named, no timing (Rule A); A6 boundary explicitly distinguished — dynamic activation column-skip, no gather, no trailing_zeros.
> Caveats to carry into the ledger row: (1) only 27 decode tokens (all 4 prompts hit EOS early: 5/7/10/5) — thin decode sample, though 74-token prefill agrees within 0.01; (2) the "prefill/decode parity 0e0 with act_stats on M7" side-claim has no committed artifact to rerun (no parity example in the worktree) — immaterial since the counters only read the buffer, but don't cite it as a verified figure; (3) z counts v==0.0 which includes -0.0 (up[i]*0.0 with negative up produces -0.0) — correct for MAC/traffic math, but a future bit-exact column-skip kernel must handle -0.0-input columns to preserve byte-identity.
> 
> === CROSS-LANE FINDING (nobody's fault, worth recording) ===
> The gemm_equivalence "pre-existing flake" is now characterized: batched_gemm_matches_per_token_matvec FAILS under default parallel libtest execution in ALL THREE worktrees (and thus at base a249b2c) but PASSES with --test-threads=1 in all three. It is cross-test interference within one test process — almost certainly another test flipping the global force-scalar/SIMD race toggle (thread_safety tests) while gemm_equivalence runs, making one path scalar vs AVX2. All three lanes' statements about it were accurate for how each ran the suite; task #25 should target the shared-process toggle, not the GEMM math.
> 
> Key files: wt1 aegis-core/{src/ops_bitplane.rs, tests/bitplane_exactness.rs, benches/bitplane_vs_lut.rs}; wt2 aegis-core/{src/ops.rs (+375), tests/fused_matvec_exactness.rs, benches/fused_vs_sequential.rs}; wt3 {aegis-core/src/inference.rs, aegis-linux/examples/act_sparsity.rs, docs/hardware_logs/relu2_act_sparsity_bitnet2b_2026-08-01.log}. No timing number from this session was recorded anywhere; all bench executions were smoke-only with identity gates checked.


## Sundown battery (MECHV2 unikernel / L-stick v3 / E2 integer path) — `wf_4b096fb8-bef`

**VERDICT: SOUND**

> Reviewed a463281 in /home/killboxincorporated/.claude/worktrees/wf_4b096fb8-bef-1. Diff is 84 pure insertions in aegis-uefi/src/main.rs, one hunk inside the existing MECH scope; v1 MECH block, mech_run! macro, H3 probe, gauntlet, and all other paths untouched (verified via git show -p and grep). Adversarial checks all held: (1) the EXACT determinism gate is meaningful — process_intent returns tokenizer.decode(&generated_tokens) only; the [PERFORMANCE] timing line goes to the discarded print_cb buffer, so byte comparison cannot spuriously fail on timing text; (2) no cross-run state leak — KV rewritten from pos 0, penalty state local, act_zero_counts pushes gated behind the off act_stats feature, so no heap growth over the 30 extra runs; (3) timed region is byte-identical in shape to v1 QUIET2 (rdtsc/snapshot ordering, same ntok filter, quiet_buf.clear() before t0); (4) log lines mirror v1 grammar and are regex-parseable; both new unsafe rdtsc blocks have SAFETY comments. I reran every gate myself in the worktree: cargo fmt --check clean; stock x86_64-unknown-uefi build exit 0 with and without --features qemu-test (E0133 lines are pre-existing unsafe_op_in_unsafe_fn WARNINGS in the allocator, unrelated); build_hardfloat.sh exit 0 with SIMD census fma=161 ymm=433, matching the builder's claim exactly. Residual caveats, already declared by the builder and not code defects: runtime log rendering unexercised (boot-test reserved to the main tree — run a qemu-test boot there before staging a stick), and the Dell runcard must budget for ~30 extra 256-token decodes of wall time. No timing recorded on this contended box.

**VERDICT: SOUND**

> Lane B verified SOUND at commit 5cff61a in /home/killboxincorporated/.claude/worktrees/wf_4b096fb8-bef-2. Independently reproduced: (1) cis_selftest example on host — 14/14 sections PASS, digest 76985613c965f643 matching both the pinned constant in aegis-linux/tests/cis_selftest_digest.rs and the QEMU-boot log; (2) cargo test --release (aegis-linux incl. pin test) and aegis-core cis::tests (11/11) pass; cargo fmt --check clean; the two clippy warnings are pre-existing baseline debt (aegis-linux/src/main.rs:17, aegis-core/src/sampler.rs top_p), not from this commit; (3) replicated generators/seeds/goldens in examples/cis_selftest.rs are bit-identical to aegis-core/src/cis.rs unit tests (compared line by line); (4) reran scripts/linux-arm/build.sh + qemu-test.sh from the worktree — both PASS exit 0, and since the gate only asserts "GAUNTLET DONE" I manually inspected the extracted BOOTLOG: machine-detect fallback line, MECHV2L headers + Prefill/Decode/[PERFORMANCE] Cycles + RESPONSE lines, CIS_SELFTEST digest identical to host, witness chain-hash transcript, membw CSV+summary, and the intact kernel A/B block are all present; (5) exercised all three machine-detect branches with realistic model-name strings — correct, and the Dell branch string is byte-identical to the previously hardcoded AEGIS_MACHINE export, so existing Dell log formats are preserved; (6) existing paths undisturbed: gauntlet L1/L2/L3 unchanged, analyze_oscost.py's L-parser is regex-bounded by "==== GAUNTLET DONE" so appended sections cannot affect it, commit touches only 4 files, no benches/unikernel/append-only paths; (7) non-AVX2 grace verified statically: all AVX code sits in #[target_feature] functions, guards at fused_vs_sequential.rs:367 (before print_clock_state and kernels) and bitplane_vs_lut.rs:199 (before make_packed/pack_bitplanes), pre-guard code is rdtsc + scalar asm + f64 under baseline x86-64 codegen (no target-cpu flags anywhere; N4020 has SSE4.2) — runtime proof impossible here (no qemu-user), matching the builder's disclosure; (8) witness/membw CLI arg conventions match the init.sh invocations; (9) Rule A/B/C compliant — no QEMU number recorded as a result, nothing added to docs/hardware_logs, KVM BOOTLOG gitignored. Residual risks are exactly the builder's disclosed open issues (Dell/HP branches and cross-ISA digest equality get first live test on the sticks; TCG timeout margin untested; MECHV2L RESPONSE first-line truncation) plus one observation: qemu-test.sh's PASS grep predates and does not cover the new sections, so future regressions there would pass the gate silently — a pre-existing gate weakness, not introduced by this commit, and compensated this time by direct BOOTLOG inspection.

**VERDICT: SOUND**

> Adversarial review of lane e2-integer (worktree /home/killboxincorporated/.claude/worktrees/wf_4b096fb8-bef-3, commit 3c82d8b). I reran everything myself and tried to break it; the deliverable survives.
> 
> VERIFIED BY RE-EXECUTION:
> 1. Reproduction: rebuilt aegis-eval (release) and ran the exact repro command as an independent 4th run — bit-identical to the log: float PPL 5.639491, integer PPL 5.657126, delta +0.3127% (kill line +5%), digest 0x42E820C2A8A59CD6. All four artifact sha256s (MODEL.SAF/EMBED.BIN/VOCAB.BIN/heldout.txt) match the log's recorded hashes.
> 2. Regression anchors: --sample (untouched code path) still prints "Perplexity (teacher-forced, 471 tokens): 5.639", matching model-lab/tinybit/m7_final_roundtrip.log line 30. The float calculate_perplexity in aegis-core/src/inference.rs is genuinely untouched (diff adds only a 7-line read-only pipeline() accessor); the mirror claim checks out line-for-line (window truncation, first-token seeding, NaN on out-of-vocab target, exp(mean NLL)).
> 3. Gates: devloop fmt PASS (6 crates), clippy ratchet exactly at baseline for all 6 crates, devloop test PASS (aegis-core 32/32). Stock x86_64-unknown-uefi build of aegis-uefi exits rc=0 (cis_infer compiles no_std into the unikernel; the E0133 lines are pre-existing warn-level unsafe_op_in_unsafe_fn lints, not errors). Did not run boot-test per lane instructions.
> 4. Golden independence (the lane-D-specific check): ran scripts/cis_e2_golden_gen.py — every emitted constant matches the Rust test constants exactly (bf16/f32 conversions incl. RNE tie cases, f32_to_ratio, all 7 QScale64 cases incl. the 2^90+12345 / 3^30 big-int case, rescale ties, normq/quantq codes+scales, head dot, FNV). The Python implementation is genuinely independent: arbitrary-precision ints, and a structurally different QScale64 algorithm (normalize-by-search vs Rust's restoring long division) — agreement is meaningful. FNV-1a 64 vectors ("a", "foobar") match the published reference values; the prime/offset constants are correct.
> 5. Rule discipline: commit is 100% additive (1599 insertions, 0 deletions); program/, tests/golden/, and all existing hardware logs untouched; the evidence log is a NEW append-only file with machine named, contention disclosed, and an explicit "wall-time lines are NOT results" Rule A disclaimer. Log format is consistent and parseable. verify-figures.sh: 18/20 substantiated; the 2 failures are exactly the disclosed pre-existing recovery_92 and smollm_59 (RANGER figures, untouched by this lane).
> 
> MINOR NITS (none block the merge or the E2 claim):
> - Report says "12 new golden tests"; the actual count is 11 tests in cis_infer::tests (32 lib tests total confirmed). Prose-only discrepancy, appears in no log or ledger.
> - sample_tokens in aegis-eval/src/main.rs is a byte-identical DUPLICATE of run_sample's window logic, not a factoring (nothing was removed; the report's "factored out" is inaccurate). Identical today — I compared line by line — but it can drift if run_sample is later edited. Suggest run_sample call the helper in a follow-up.
> - Latent robustness edge, relevant before the BitNet-2B leg: QScale64::from_ratio documents the precondition num,den ≤ 2^127 but does not assert it, and residual_qscale's checked_mul/checked_shl guards only reject >2^128 — a 128-bit operand would silently lose its MSB in the `rem <<= 1` restoring-division loop. Unreachable with M7's asserted value ranges (|h|<2^45, |g|<2^31 keep num ≤ ~2^112), but not provably impossible for exotic checkpoints (e.g. subnormal weight scales pushing den<<(-sh) past 127 bits). A one-line assert in from_ratio would close it.
> 
> The open_issues list in the builder report is honest and accurate (ledger deferred to integrator, cross-kernel-path bit-identity correctly NOT claimed, BitNet-2B leg still open under task #9).


## Edge body (colskip / e2-bitnet / int-attention) — `wf_bca6e090-de7`

**VERDICT: SOUND**

> Lane colskip verified at /home/killboxincorporated/.claude/worktrees/wf_bca6e090-de7-1, commit ad9eab409850e2c8282e152e7fd9e23811d6e207. All lane-specific attacks run and defeated. (1) fma exactness: fma_zero_skip_identity covers BOTH zero signs x in {+0.0,-0.0} and ALL ternary codes w in {-1,0,+1} on both libm::fmaf and _mm256_fmadd_ps, proves -0.0-accumulator unreachability (invariant, not assumption), proves hw==libm bitwise, and pins the fma(+0,1,-0.0)=+0.0 boundary; end-to-end the skipping kernels are asserted byte-equal to NON-skipping scalar mirrors on -0.0-laced inputs, and the ordered variant is asserted byte-identical to the incumbent ternary_matvec_avx2 — I reran all 8 tests, green, and structurally confirmed the lane-class (col%8) / fold / tail-quad / scale analysis against ops.rs. (2) Real vectors: independently parsed the .av1 artifact (magic, dim 6912, 750 records, layers 0-29, pooled z=0.7886, 1,926,255 bitwise -0.0) — matches the committed log and A15; bench loads it (94 stride-sampled vectors in my smoke run), refuses a verdict without it, and all pre/post bit-exactness gates held. (3) Repack round-trip verified per (row,col) code incl. injected 0b11 on 7 shapes. (4) No A6 smuggling: one scalar x==0.0 compare per column, no trailing_zeros/tzcnt; 2 bits/weight preserved so traffic <= incumbent (not A7 either). Full aegis-core suite green (rerun, --test-threads=1); fmt clean; aegis-uefi compiles for x86_64-unknown-uefi; clippy aegis-core at baseline (10); aegis-uefi 50 vs baseline 49 CONFIRMED pre-existing (I ran the ratchet at parent 24085f0 in a temp worktree: also 50 — integrator should rebaseline or pay down, not this lane's debt). Rules A/B/C clean: no ledger touch, only a new hardware_logs file with values/counts (no timing), smoke timings not recorded as results. Non-blocking nit: bench header claims post-timing gates on every vector; the post gate actually runs on the last vector only (pre gates do run on all) — accurately documented at the call site, could be reworded at merge. Open issues in the builder report check out as disclosed (25 decode tokens vs nominal 32 with matching z; 20.7 MB committed artifact; Dell L-stick A/B is the integrator's step).

**VERDICT: SOUND**

> Worktree /home/killboxincorporated/.claude/worktrees/wf_bca6e090-de7-3, commit df89da8. All headline claims reproduced independently, including from a from-scratch rebuild in a fresh CARGO_TARGET_DIR: float PPL 5.639491, hybrid 5.657126 (+0.3127%), full-int 5.643085 (+0.0637%), digests 0x42E820C2A8A59CD6 (hybrid, exact match to the E2/A19 log — hybrid path verified untouched, residual_qscale is a bit-identical delegation to fixed_qscale) and 0xBED4A17A1A5EE296 (full-int), matching both runs in docs/hardware_logs/cis1_fullint_attention_ppl_m7_i5-10210U_crosvm_2026-08-01.log. Artifact sha256s verified against the log; sample window pinned and deterministic (471 tokens). Regenerated scripts/cis_e2_golden_gen.py: every printed v0.3 constant/vector/digest matches the Rust goldens (chain 0x6CE6217F7A809D74, LUT 0x66C2A0EEB8C2DC43, RoPE table 0xD8345EBF01E990FA). Sum-to-one (|Σp−2^15| ≤ ⌈T/2⌉ at n=2..512), full pairwise softmax monotonicity, LUT strict decrease, and 50k-point exp monotonicity are all hard-asserted; FullInt headroom guards are real assert! and no existing assert was weakened. Tests: aegis-core 76 passed 0 failed (15 cis_attn tests ran); fmt PASS; clippy at baseline except the disclosed pre-existing aegis-uefi 50-vs-49 (lane touched zero uefi files); verify-figures fails only the two disclosed pre-existing RANGER figures. No A6-boundary bit-scan; Rule C clean (new log only, spec pure-append); Rule A respected (QUALITY-ONLY banner). Two minor non-blocking nits for the integrator: (1) two golden inputs are absent from the committed generator's printed output — exp_neg_q31(21<<32)=2 and the silu seam at u=2^20 (generator prints u=2^30); I re-ran the generator's own big-int functions on the exact Rust inputs and both values are correct, but a two-line generator addition would make the "all goldens transcribed from generator output" claim literally true. (2) rope_apply_i silently no-ops when seq_pos >= max_seq instead of panicking — unreachable here (window capped at max_position_embeddings) but contrary to the loud-assert philosophy; suggest converting to an assert in a follow-up.


## fable-hand v2 (own-the-box) — `wf_6358b815-ae1`

