# Adversarial review — CIS-1 paper draft, 2026-08-27

Reviewer stance per `docs/ADVERSARIAL_REVIEWS_2026-08-01.md`: re-verify against
primary sources, don't trust self-report, treat every number/claim as guilty
until traced to a ledger row, spec section, or a re-run artifact. Rule B (no
number in the ledger without a raw log) is the standard applied here to the
*paper*, not just the ledger: a number in prose must trace to a specific
ledger row's actual verdict, not a looser or more flattering paraphrase of it.

**Scope reviewed:** `docs/paper/01_introduction.md`, `07_why_it_matters.md`,
`08_limitations.md`, `02_related_work.md`, against `program/RESEARCH_LEDGER.md`
rows A19–A34, `docs/CIS-1_SPEC_v1.0.md` §10 (and adjacent normative sections
where §10 turned out not to contain the cited material), and
`docs/paper/table3_provenance.md`.

**Operational note:** mid-review, this working tree's HEAD moved from
`cm/paper` to `main` without my initiating it (a `git status` returned "On
branch main" after an earlier `git checkout cm/paper` had confirmed I was on
it). `docs/paper/` does not exist on `main`. I checked out `cm/paper` again
before continuing and the rest of this review was performed there. Flagging
this because it means something else touched this working tree concurrently;
worth confirming before merge that no other session raced this one.

**Citation spot-check (task d):** fetched the arXiv abstract page for 6 ids
cited in `02_related_work.md` (2402.17764, 2504.12285, 2410.16144, 2511.20621,
2604.02292, 2101.01321). All 6 exist and match the title/authors/claims
attributed to them, including the one the draft itself flags "UNVERIFIED
beyond its abstract" (2604.02292 — that self-flag is accurate and honest, not
an error). **`02_related_work.md` is clean on citation existence and
citation-content accuracy.** One low-materiality nit noted below on framing,
not citation fact.

---

## Findings

| File | Quote (≤20 words) | Problem | Ledger/spec evidence | Suggested fix |
|---|---|---|---|---|
| `07_why_it_matters.md` | "runs a 2-billion-parameter ternary model on a decade-old laptop with no operating system" | **Compound claim no single row supports.** The no-OS/unikernel receipt demonstration (A32–A34) was performed on the M7 model only. BitNet-2B's quality gate (A21) ran on i5-10210U **crosvm**, and BitNet-2B has never been booted on the UEFI unikernel; A20 lists "BitNet-2B FullInt needs KV-cache footprint work (~630MB at 4096 pos)" as still open. | Spec §9: "production-scale ternary model (BitNet-2B) +0.74% in the integer-dominant *hybrid* configuration... **2B full-integer quality is not yet measured.**" A32–A34 (receipt/no-OS) all cite the M7 receipt (`witness_v1_m7_once64.receipt`), never BitNet-2B. | Split the claim: state separately that the receipt/no-OS result is proven for M7 (A32–A34) and that BitNet-2B's quality gate passed under crosvm (A21) — do not merge them into one sentence implying BitNet-2B ran with no OS. |
| `01_introduction.md` | "the all-integer forward pass costs +0.06%... and +0.74% on a 2-billion-parameter ternary model" | **Mislabels a hybrid-path number as all-integer.** A21's BitNet-2B +0.7408% figure is the **hybrid** (f32 attention) configuration, not full-integer — A21's own text lists "Open: BitNet full-int (KV footprint)" as still outstanding. | Spec §9 states this explicitly: BitNet-2B "+0.74% in the integer-dominant *hybrid* configuration (f32 attention — outside §7 conformance; ledger A21). 2B full-integer quality is not yet measured." (Root cause: `table3_provenance.md` row 6 itself mislabels this as "full-integer path, BitNet-2B" — the paper appears to have inherited the mislabel from its own provenance table.) | Reword to: "+0.06% on a 384-hidden reference model (full-integer, A20) and +0.74% on the hybrid-configuration BitNet-2B leg (A21); full-integer BitNet-2B quality is not yet measured." Also fix `table3_provenance.md` row 6's label (outside this review's edit scope but flagged). |
| `07_why_it_matters.md` | "the integer path is faster than the floating-point path on the hardware where it was measured" | **Blanket overclaim contradicted by the project's own data.** A26 measured integer **25% slower** than scalar float on Dell (Broadwell-U, C/B 1.248×); only HP (Goldmont Plus) was faster (4–14%) at scalar parity. The "faster" result on Dell (A27, 2.94×) is a *different* comparison — AVX2 integer kernel vs. AVX2 float kernel, not scalar vs. scalar. | A26: "Dell i5-5200U (Broadwell-U): C/B median 1.248x... integer semantics cost 25% against scalar float... Both directions occur; the semantics do not dictate either." (also spec §9, same wording) | Qualify by machine and comparison axis, matching `01_introduction.md`'s own more careful phrasing ("cost 25%... on a Broadwell-U core and are 4–14% faster on a Goldmont Plus core... at parity vector width the integer AVX2 kernel is 2.94× faster"). Don't collapse this into one unqualified "faster" sentence. |
| `01_introduction.md` | "Two independent implementers, given only the specification and denied access to the reference source" | **"Independent" without disclosing they were LLM subagents, and "only the specification" omits the public conformance harness/goldens they also used.** This is exactly the ledger-vs-prose gap the review brief calls out by name. The paper's own §8 later discloses "LLM subagents" — but item 1 of the intro, which is what gets quoted/circulated, doesn't. | A31: "Two independent implementers (**subagents**, different personas)... **Honest scope:** the public conformance suite's golden vectors were visible to the implementers... so the proven claim is 'implementable from spec text **+ public conformance suite**, without reference source,' not from prose alone." | Add "(LLM subagents)" in the intro's item 1, and note the conformance-suite dependency there too, not only in §8 — a reader who stops at the intro should get the accurate scope. |
| `08_limitations.md` | "Scope of the semantics (spec §10)." | **Wrong section citation.** `docs/CIS-1_SPEC_v1.0.md` §10 ("Not claimed by v1.0") contains no hidden-size/head-dim/sequence-length bounds — it lists seeded sampling, 2B conformance artifacts, witness format, machine attestation, training semantics, and primitive novelty. The 384–2560/≤128/≤512 numbers do not appear anywhere in the frozen v1.0 spec file; they trace only to ledger row A20's prose ("spec v0.3 notes"), i.e. `CIS-1_SPEC_DRAFT_v0.1.md`'s implementation notes, which the frozen spec explicitly calls "design history," not normative. | Confirmed by direct grep of `docs/CIS-1_SPEC_v1.0.md`: no occurrence of "384", "2560", or a stated seq-length/head-dim scope bound anywhere in the file. A20: "numerical-safety bounds derived in **spec v0.3 notes** for hidden 384–2560, head_dim ≤128, seq ≤512." | Either (a) cite the correct source (ledger A20 / `CIS-1_SPEC_DRAFT_v0.1.md` v0.3 notes) instead of "spec §10", or (b) if these bounds are meant to be normative, add them to v1.0 §3 first and cite that. |
| `01_introduction.md` | "The op-level digest is identical on five x86 execution paths" | **Count is defensible but under-sourced as written.** The enumeration that follows names only 4 things (two bare-metal machines, one virtualized host, one emulation), and the two ledger rows cited (A25, A28) don't establish a 5th x86 leg — A25 itself says "**Four** distinct microarchitectures + codegen paths." The 5th leg (x86-64 GitHub CI) is real but lives in A29, which isn't cited in this sentence. | A25: "Four distinct microarchitectures + codegen paths, one bit-exact integer semantics." Spec header/§9 independently corroborates "six execution environments" total (5 x86 + 1 aarch64) by naming the x86-64 CI host explicitly — but that's the *spec's* accounting, not what's cited here. | Either name the x86-64 CI host explicitly in the enumeration and cite A29 alongside A25/A28, or reduce the count to match what A25 alone supports. |
| `07_why_it_matters.md` | "Every result in this paper is a digest or a logged measurement on a named physical machine" | **Ambiguous phrasing reads as "every result is physical-hardware-sourced," which is false** for the aarch64 CI legs (A28–A32, hosted cloud runners) and the crosvm dev-host legs (A19–A21, A31) — `08_limitations.md` itself distinguishes these as "identity evidence only," explicitly not physical-machine measurements. | `08_limitations.md`: "**Cloud-runner legs are identity evidence only.** The aarch64 results (A28, A29, A30, A32) were obtained on hosted CI runners." A25 separately enumerates "HP N4020 bare iron, Dell i5-5200U bare iron... crosvm dev host... QEMU/TCG emulation" as four distinct, non-interchangeable categories. | Reword to avoid the misreading, e.g.: "Every number in this paper is a digest (from a named machine or CI host) or a timing measurement logged on a named physical machine" — keep the digest/timing distinction explicit. |
| `01_introduction.md` | "no one can say exactly what a deployed model computed on a given input" | **Nit — sweeping absolute in the motivational hook**, not tied to any ledger row so not independently falsifiable, but "no one can say" is the kind of totalizing phrase a hostile reviewer flags on reflex. | N/A (framing, not a ledger claim) | Soften to "it is hard to say" or scope it to the floating-point baseline being critiqued. |
| `01_introduction.md` | "is not comparable to published numbers" (re: the 2B perplexity figure) | **Nit — narrower than the ledger's own caveat.** A21 flags non-comparability against *both* external published numbers *and* this project's own 1,898-token/10.758 anchor; the intro's "what's not shown" recap only mentions "published numbers," dropping the internal-anchor half that `08_limitations.md` correctly keeps. | A21: "200-token window on an <unk>-dense region — **NOT comparable to the 1,898-token 10.758 anchor**"; `08_limitations.md` correctly states both. | Low priority since `08_limitations.md` carries the full caveat — but for consistency, either match its wording or drop the redundant partial recap in the intro. |

## Clean

**`02_related_work.md`** — clean. All 6 spot-checked arXiv citations (out of
5+ required) resolve to real papers whose titles, authors, and claimed
figures (e.g. Wang et al.'s 2.37–6.17×/1.37–5.07× CPU speedups) match what's
attributed to them. No CIS-1 ledger numbers are cited in this section to
cross-check. One low-materiality framing point, not logged as a table row to
avoid padding: "None of this line of work specifies a bit-exact numerical
contract..." is a strong, unqualified claim about the *absence* of a property
across an entire literature — true as far as I can check, but it's an
assertion about what other papers *don't* do, which is harder to falsify than
what they *do* claim, and is exactly the shape of statement a hostile
reviewer will try to find one counterexample against.

## Severity counts

- **Must-fix: 5**
- **Should-fix: 2**
- **Nit: 2**

## Three most important

1. **`07_why_it_matters.md`'s "2B model on a decade-old laptop with no
   operating system"** — the single most damaging finding. No ledger row
   supports it; it directly contradicts spec §9's explicit "2B full-integer
   quality is not yet measured," and BitNet-2B has never run on the no-OS
   unikernel at all (only M7 has, via A32–A34).
2. **`01_introduction.md`'s "+0.74%... all-integer forward pass"** mislabels
   the BitNet-2B **hybrid** result as full-integer, contradicting
   `CIS-1_SPEC_v1.0.md` §9 verbatim — and the error traces back to
   `table3_provenance.md`'s own row 6 label, so it's a propagated error, not
   an isolated slip.
3. **`07_why_it_matters.md`'s blanket "faster... on the hardware where it was
   measured"** — directly contradicted by A26's Dell scalar result (integer
   25% *slower* than float on Broadwell-U); the paper's own introduction
   states the nuanced (correct) version two sections earlier, so §7
   contradicts §1.


## Disposition (2026-08-27, Fable)
All 5 must-fix, 2 should-fix and 2 nits applied in the same commit: 2B +0.74% relabelled HYBRID everywhere (intro, outline, Table 3 generator); the "2B on a laptop with no OS" compound claim replaced by the two separately-supported claims (M7 receipt on iron A33/A34; 2B in the Linux harness A21) with the gap stated; the "faster" sentence replaced by the A26/A27 decomposition; implementers described as LM agents with harness access; §8 scope citation corrected to spec §3 + A20/A21; "five x86 paths" → four (A25); reproducibility sentence now distinguishes CI digests from physical measurements; both nits applied. Branch-flip note: that was the operator switching the working tree to main mid-review — no concurrent session.

## Pass 2 (sections 3–6, 9)

**Scope reviewed:** `docs/paper/03_semantics.md` against
`docs/CIS-1_SPEC_v1.0.md` (every number/digest/section citation checked
verbatim); `docs/paper/04_identity.md` and `05_receipt.md` against ledger
rows A25, A28–A34; `docs/paper/06_cost_quality.md` and `09_reproducibility.md`
against rows A19–A24, A26, A27, `CLAUDE.md` Rules A–D, and the literal text
of `.github/workflows/arm-digest.yml`; cross-checked against
`01_introduction.md` and `08_limitations.md` for contradictions.

**Operational note (recurrence).** The same hazard Pass 1 flagged recurred
twice in this pass: partway through, and again immediately before this
section was appended, `git status` reported `On branch main`, where
`docs/paper/` does not exist — both times after a preceding `git checkout
cm/paper` had confirmed the branch. Both times `cm/paper` was re-checked-out
before continuing. This has now happened in two independent review sessions;
it is no longer a one-off and is worth root-causing (a hook, a concurrent
session, or an alias) before it silently drops a review's edits on the wrong
branch.

**Untracked concurrent evidence, not yet in the ledger.** Two untracked
files exist at `docs/hardware_logs/cis1_fullint_ppl_bitnet2b_i5-10210U_crosvm_2026-08-27.log`
and `..._r2.log` — an in-progress attempt (operator "Claudius Maximus
(Fable)", same date as this review) to close exactly the gap this pass was
told to police: BitNet-2B full-integer perplexity. Both logs are incomplete
(`r2` ends mid-transcript with no PPL printed, `RUN 1` in the first file
failed at exit 127 with no measurement). Neither file has a matching
`program/RESEARCH_LEDGER.md` row. **As of this review, the paper's "2B
full-integer quality is not yet measured" claims (06, 08, and cross-checked
in 01) remain accurate** — but this is live, unlanded work on the exact
claim this pass was asked to police, and it should be re-checked before
merge in case a completed run lands a number into the ledger without going
through review.

| File | Quote (≤20 words) | Problem | Ledger/spec evidence | Suggested fix |
|---|---|---|---|---|
| `08_limitations.md` | "Scope of the semantics (spec §3 and §10; bounds as derived in ledger A20–A21)." | **Still-wrong section citation — the Pass-1 fix didn't fix it.** `grep -n "384\|2560\|head_dim" docs/CIS-1_SPEC_v1.0.md` returns zero matches: hidden 384–2560, head_dim ≤128, and seq ≤512 appear **nowhere** in the frozen v1.0 spec, neither §3 (the data-types table) nor §10 (which lists sampling/2B-artifacts/witness-format/attestation/training/novelty — no numeric bounds at all). The numbers exist only at `docs/CIS-1_SPEC_DRAFT_v0.1.md:390-391` ("v0.3-6. Full-integer engine grids and headroom (hidden 384–2560, head_dim ≤ 128, seq ≤ 512)"), which v1.0's own header calls "design history" that is not normative when it disagrees with — or, here, simply isn't present in — the frozen document. | `docs/CIS-1_SPEC_v1.0.md` §3 (full data-types table, no hidden/head_dim/seq bounds), §10 (full "Not claimed" list, no numeric bounds); `docs/CIS-1_SPEC_DRAFT_v0.1.md:390-391` (actual source); ledger A20 ("numerical-safety bounds derived in spec v0.3 notes for hidden 384–2560, head_dim ≤128, seq ≤512"). | Drop the "spec §3 and §10" citation entirely — it cites sections that don't contain the claim. Cite ledger A20 (and, if the range extension to 2560 needs its own citation, A21) pointing at the DRAFT's v0.3 notes, and say plainly that these bounds are design history, not yet promoted into the frozen spec. |
| `09_reproducibility.md` | "Commands (quoted verbatim from `.github/workflows/arm-digest.yml`...)" | **Not verbatim.** The actual `digest-x86` job never runs `./aegis-linux/target/release/examples/cis_selftest` or the `cis_decode` invocation bare — both are piped through `grep '^CIS_SELFTEST '/'^CIS_DECODE ' \| tee /dev/stderr \| grep -q '...' \|\| { echo '::error::...'; exit 1; }`, which the paper strips entirely. The paper's witness block also omits the `M=model-lab/tinybit/m7_final_gate_work/artifacts` line the workflow re-declares immediately before that step (line 156), and the paper's `cis_decode` build line has one space before `--manifest-path` where the actual x86 job (line 140) has two. None of the paper's four command blocks appears as a contiguous, unmodified excerpt anywhere in the file. | `.github/workflows/arm-digest.yml` lines 139–159 (`digest-x86` job, the actual source for "runs them on... both `ubuntu-24.04` and `ubuntu-24.04-arm`" — though note this job itself only runs on `ubuntu-24.04`; the ARM job is `digest`, lines 28-117, whose invocations are wrapped in `OUT=$(...)` + separate `grep` checks, equally not bare). | Either reprint the actual grep/tee/exit-1 lines so the block really is verbatim, or drop the word "verbatim" and call it "reconstructed from" / "equivalent to" the workflow's commands — the functional content is correct, only the verbatim claim is false. |
| `04_identity.md` | "**A NEON kernel bit-identical...(A30).** On the same date," | **Date drift.** This sentence follows the A31 paragraph, which opens "On 2026-08-08 two independent implementers..." — so "the same date" reads as 2026-08-08. Ledger A30's own header is "NEON CIS-1 KERNEL BIT-IDENTICAL ON REAL ARM SILICON **(2026-08-07)**" — the same day as A28/A29, one day before A31, not the same day as A31. | `program/RESEARCH_LEDGER.md` A28 "(2026-08-07)", A29 "(2026-08-07)" (Objective 1, first leg), A30 "(2026-08-07)" (Objective 2, first leg), A31 "(2026-08-08)". | Change "On the same date" to "The same week" or "On 2026-08-07" (matching A28/A29, which the NEON paragraph is chronologically adjacent to, not A31). |
| `03_semantics.md` | "(`cis_decode`, prompt `\"Once upon a time\"`, 64 new tokens, greedy, `FullInt`)" | **Nit — drops "EOS ignored" from the Tier 3 procedure.** Spec §8 Tier 3 states the run as "prompt 'Once upon a time', 64 new tokens, greedy, **EOS ignored**, FullInt" — the paraphrase in §3 omits this design choice. Not misleading on its own (the correct caveat is stated in full in `04_identity.md`'s A29 paragraph: "greedy decoding only, EOS ignored... with no stop-condition dependence"), but it's an inconsistent level of detail for what's introduced as an exact quote-adjacent procedure description. | `docs/CIS-1_SPEC_v1.0.md` §8: "Run `cis_decode` on the in-repo M7 artifacts..., prompt 'Once upon a time', 64 new tokens, greedy, EOS ignored, FullInt." | Add "EOS ignored" to the parenthetical for consistency with §4's fuller statement of the same fixed procedure. |

## Clean

**`03_semantics.md`** — every pinned constant, digest, and normative-section
citation checked against `docs/CIS-1_SPEC_v1.0.md` character-for-character:
`TWO_PI_Q62`/`PI_Q62`/`PI_2_Q62`/`LOG2E_Q32`, the exp-LUT digest
`0x66C2A0EEB8C2DC43`, the RoPE digest `0xD8345EBF01E990FA`, both conformance
digests, every grid/bound in the §3 table, the §5.1 five-precondition count,
and the §5.12 pipeline table all trace exactly to the spec text. One nit
above (dropped "EOS ignored"); otherwise clean.

**`06_cost_quality.md` / Table 4** — every figure and digest
(`0xBED4A17A1A5EE296`, `0x42E820C2A8A59CD6`, `0x24C4E510A86659D6`, 78×
headroom, 1.248×/0.961×, 4.61×, 0.340×/2.94×, 0.061×/16.4×, +3.6%/+9.4%/+5.1%,
P-V2-2's 4.7–9.6%, 2.88–2.89×/2.80×, 11.19–11.70 GB/s vs 0.62 GB/s) checked
digit-for-digit against A19–A27 and matched. This section correctly labels
A21 as HYBRID throughout (the Pass-1 must-fix from `01_introduction.md`/
`table3_provenance.md` did not recur here) and never lets a QEMU/TCG or
absolute-cycle figure appear, consistent with Rule A/Rule D.

**`05_receipt.md`** and the A32/A33/A34 paragraphs of `04_identity.md` —
every hash prefix, run ID, and snapshot (`31249589879`, `ce93bbb`,
`87c45bdd…`, `320e1918…`, `aee25b770bd7b22e…`) checked against the ledger and
the two provenance notes (`docs/hardware_logs/dell_..._provenance_2026-08-08.md`,
`hp_n4020_..._provenance_2026-08-08.md`) and matched; the attribution
caveats (memory-map evidence for Dell, operator-witness-only for HP) are
carried faithfully, including into `08_limitations.md`, with no contradiction
found across sections.

**No BitNet-2B all-integer overclaim found.** Every place this pass checked
that could have implied 2B full-integer quality was measured (`06`, `08`,
and by cross-check `01`) states the negative correctly and cites spec §9/§10
or A20/A21 for it. See the untracked-evidence note above for why this needs
a re-check before merge, not because the current text is wrong.

## Severity counts (Pass 2)

- **Must-fix: 2**
- **Should-fix: 1**
- **Nit: 1**

## Three most important (Pass 2)

1. **`08_limitations.md`'s "spec §3 and §10" citation is still wrong.** This
   is the same defect Pass 1 found (previously "spec §10" alone); the
   disposition's fix changed the citation but not its correctness —
   `grep` confirms the cited bounds (hidden 384–2560, head_dim ≤128, seq
   ≤512) appear in neither §3 nor §10 of the frozen v1.0 spec, only in the
   DRAFT's non-normative v0.3 notes. A citation fix that still doesn't point
   at the right document is worse than leaving the wrong section number,
   because it looks resolved.
2. **`09_reproducibility.md` claims commands are "quoted verbatim" from
   `arm-digest.yml` and they are not.** The actual workflow wraps every
   digest check in `grep`/`tee`/explicit-`exit 1` error handling that the
   paper silently strips, and the witness block drops a repeated `M=`
   assignment the real job re-declares. The commands are functionally
   equivalent, but the specific, falsifiable claim of verbatim quotation is
   false — exactly the kind of claim this review's standard treats as
   guilty until traced to source.
3. **`04_identity.md` misdates A30 to 2026-08-08 via "on the same date"**
   when the ledger's own A30 header says 2026-08-07 — a day earlier, and the
   same day as A28/A29, not A31. Lower stakes than 1–2 (no substantive claim
   changes) but a factual date is exactly the category this review is
   chartered to catch.

### Disposition pass 2 (Fable, 2026-08-27)
All four applied: scope envelope now attributed to A20–A21 + draft v0.3 notes (not the frozen spec); A30 dated 2026-08-07; §9 commands described as invocation cores with the grep/tee wrappers and the M re-declaration called out; Tier-3 "EOS ignored" restored. Branch flips: operator checkouts between agent runs — now using worktrees. The two 2026-08-27 hardware logs are experiment E1 (2B full-integer gate) in progress; the paper will be updated with its result either way before merge.

## Pass 3 (A35 integration)

**Scope:** integrate ledger row A35 (`program/RESEARCH_LEDGER.md`, 2026-08-27)
and spec erratum v1.0.3 (`docs/CIS-1_SPEC_v1.0.md` §5.10 + §11) — the result
Pass 2's disposition flagged as in-progress ("the paper will be updated with
its result either way before merge"). A35 landed: the complete all-integer
BitNet-2B forward pass measures **+0.1239% PPL** vs. float (closer to float
than the A21 hybrid's +0.7408%), after fixing a real spec gap in §5.10 (no
§5.6 block exponent on the ACT-I output) that the unfixed binary hit as a
NORMQ panic, 2/2 runs. Self-check: every sentence changed, listed against its
ledger/spec citation.

| File | Sentence changed | Citation |
|---|---|---|
| `01_introduction.md` | Contribution 5: added the A35 full-integer figure (+0.1239%, "closer to float than the hybrid figure... 40× inside the kill line") and the negative half (NORMQ panic, §5.10 spec gap, v1.0.3 erratum fix, digests unchanged); removed "The all-integer quality of the 2B model has not yet been measured (spec §9)." | A35; spec §5.10, §11 |
| `01_introduction.md` | "What is not shown" paragraph: "2B-parameter perplexity figure" → "2B-parameter perplexity figures (hybrid and all-integer)... are not comparable" (pluralized to cover both A21 and A35, caveat unchanged in substance) | A35 (new figure added to an existing caveat); A21 (caveat unchanged) |
| `06_cost_quality.md` | A21 paragraph: removed "The all-integer forward pass has not been run on BitNet-2B at all; 2B full-integer quality is not yet measured (spec §9, ... §10)." A21's own +0.7408% figure, HYBRID labeling, and caveats (pruned vocab, 200-token window, non-comparable) are unchanged. | A35 (justifies removing the now-false "not yet measured" clause); A21 (untouched claim) |
| `06_cost_quality.md` | New paragraph: full-integer BitNet-2B measured +0.1239% PPL (30.744724 vs 30.706665), 199 scored tokens, sha-identical artifacts/window as A21, argmax digest `0xB274DE03F5862DB7`, two runs identical; unfixed binary panicked 2/2 (`normq: residual out of range`, `cis_infer.rs:313`); env-gated trace localized fault to MLP (ACT-I relu² output at 52–55 bits vs. attention re-entry exact ≤38 bits); fix = per-vector block exponent on ACT-I output, ratified as spec erratum v1.0.3; `CIS_SELFTEST 76985613c965f643` / `CIS_DECODE 67e8c0a96abc04e1` unchanged; A21 caveats apply identically. | A35 (verbatim); spec §5.10, §11 (erratum text) |
| `06_cost_quality.md` | Table 4: added row "+0.1239% \| FULL-INTEGER-path PPL cost, BitNet-2B, after v1.0.3 erratum (30.744724 vs 30.706665) \| i5-10210U crosvm \| A35" | A35 |
| `08_limitations.md` | Perplexity-caveat paragraph: "The 2B-parameter integer-vs-float comparison (A21)" → "the hybrid path (A21) and the complete all-integer path after the v1.0.3 erratum (A35)"; kept pruned-vocab / 200-token-window / not-cross-comparable wording; added "or with each other across runs beyond what the shared, sha-identical artifacts and window support." No sentence in this file previously claimed 2B full-integer was unmeasured (checked by grep; none found to remove). | A35; A21 (caveat text unchanged); spec §11 |
| `03_semantics.md` | ACT-I paragraph: added one sentence — "Since the v1.0.3 erratum, the ACT-I output MUST also carry the §5.6 per-vector block exponent (`shift = max(0, bits(max\|v\|) − 49)`) before entering NORMQ, identity at M7 ranges (§5.10, ledger A35)." | spec §5.10 (erratum text, verbatim shift formula); A35 |
| `07_why_it_matters.md` | "Economics of edge AI" paragraph: added "The complete all-integer forward pass on that 2B model now passes its preregistered quality gate (+0.1239% perplexity against float, 40× inside the +5% kill line, A35)"; kept "though the 2B model has not yet been booted on the unikernel itself; no ledger row yet establishes that." | A35; (negative: no ledger row for 2B-on-unikernel — confirmed absent from `program/RESEARCH_LEDGER.md`) |
| `docs/CIS1_PAPER_OUTLINE.md` | §7 bullet added: 2B all-integer forward pass clears its quality gate (+0.1239% PPL, A35, after v1.0.3 erratum) but has not been booted on the unikernel (no ledger row yet). | A35 |
| `docs/paper/gen_table3.py` | `CLAIM_MAP` row for A21: dropped stale trailing clause "— 2B all-integer not yet measured (spec §9)" from the claim description (the +0.7408% value/digest/row id are unchanged). Added new row: `("6", "Quality cost, FULL-INTEGER path, BitNet-2B (after v1.0.3 erratum)", "+0.1239% PPL (30.744724 vs 30.706665), argmax digest 0xB274DE03F5862DB7", "A35")`. Added `"A35": "i5-10210U crosvm"` to `extract_machine`'s known dict. Updated the two "rows A19-A34" doc comments to "A19-A35". | A35 (new row + stale-clause removal); A21 (label/value untouched) |
| `docs/paper/table3_provenance.md` | Regenerated mechanically by `gen_table3.py` from the `CLAIM_MAP` change above; new row confirms the A35 primary log (`docs/hardware_logs/cis1_fullint_ppl_bitnet2b_i5-10210U_crosvm_2026-08-27_e1c_fixed.log`) exists on disk. `python3 docs/paper/gen_table3.py --check` → `OK: all 17 claims have existing primary provenance files.` | A35; mechanical regeneration, no hand edit |

**No claim outside what A35 justifies was touched.** Every A21 number
(+0.7408%, digest `0x24C4E510A86659D6`), every M7 figure (A19, A20), every
cost/throughput figure (A22–A27), and every identity/receipt claim (A25,
A28–A34) is byte-identical to Pass 2's reviewed text. The only new numbers
introduced anywhere in this pass — +0.1239%, 30.744724, argmax digest
`0xB274DE03F5862DB7`, the NORMQ panic message and file:line, the 52–55 bit /
≤38 bit trace figures, `shift = max(0, bits(max|v|) − 49)`, and the two
conformance digests confirmed unchanged — are all quoted verbatim from ledger
row A35 and spec §5.10/§11.
