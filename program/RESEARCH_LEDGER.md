# Research Ledger — verified gains only

One line per claim. A claim enters this file only with a provenance path
(log, commit, or results file). Verdicts: ✅ CONFIRMED (primary source),
🟡 PLAUSIBLE (secondary only), ❌ REFUTED/RETRACTED (kept for the record —
negative findings are deliverables).

## A. ALICE engine (BitNet b1.58-2B-4T, no_std Rust, bare metal)

| # | Claim | Verdict | Provenance |
|---|-------|---------|------------|
| A1 | Coherent inference in userspace AND from UEFI firmware with no OS. **⚠️ THE CITED 2026-07-09 LOG IS QEMU, NOT BARE METAL** — virtualised OVMF firmware, not physical iron; the row read as if one log proved both. Real-hardware coherence is a *separate, later* result: the Dell i5-5200U boots from firmware and answers correctly (commits 20f773e, a249b2c). Cite the right log for the claim being made. | ✅ (both, on separate evidence) | QEMU: aegis-uefi/qemu_success_2026-07-09.log · bare metal: docs/hardware_logs/m7_baremetal_prompts_postfix_2026-07-29.log, bitnet_baremetal_postfix_2026-07-29.log |
| A2 | Tokenizer byte-level encode fix: WikiText-2 PPL 14.488 → 12.801, same weights. **⚠️ BOTH ABSOLUTE FIGURES ARE SUPERSEDED AND THE 12.801 ANCHOR IS UNREPRODUCIBLE — its dataset file was deleted** (docs/TECHNICAL_REPORT.md §2 row 6). Current anchors are A9 (full test set 15.825 / sample 10.348). What survives is the *direction and rough size* of the fix's effect at the time; never quote 12.801 as a current quality number. | ✅ delta / ❌ absolutes | commit history, docs/TECHNICAL_REPORT.md §2 row 6 + §heading 8 (lines 150–152) |
| A3 | Batched prefill GEMM: 1.63–1.75× single-thread, bit-identical outputs | ✅ | tests/gemm_equivalence.rs + A/B bench in commit msg |
| A4 | Multicore decode 2.62 → 5.61 tok/s = **2.14× @4t** (userspace, i5-10210U dev host — NOT the throttled Dell, so A12 does not touch it). **⚠️ THE 8.25 tok/s @8t FIGURE HAS NO LOG ANYWHERE** — this program's own audit said so in commit e659a1f: *"the multicore claim of 8.25 tok/s has no log anywhere, so the defensible figure is 2.14x."* The ledger kept carrying it as ✅ regardless, and **docs/TECHNICAL_REPORT.md §2 row 3a still headlines it in bold — fix there too.** **⚠️ CORRECTION 2026-07-29, SECOND PASS — 2.14× IS NOT "LOGGED" EITHER, AND AN EARLIER EDIT ON THIS ROW SAID IT WAS.** A deterministic provenance sweep found **no log file anywhere** recording a multicore thread sweep: no `docs/hardware_logs/` artifact, and no multicore bench exists (`aegis-core/benches/` holds only ctz_vs_simd, gemm_tile, lut_mpgemm). The entire multicore result — 2.62 / 4.77 / 5.61 / 5.40 tok/s — exists **only as prose in commit 154f00a's message**, which is the same evidence class that got 8.25 retracted. The surviving distinction is narrower than "logged vs unlogged": **8.25 is CONTRADICTED by the only primary record** (154f00a says 8t = 5.40 tok/s, *slower* than 4t), whereas 2.14× *is* that record. Both await instrument output. The "SMT helps by ~5%" conclusion is contradicted outright. **A multicore thread-sweep bench emitting a log is an open task, and until it exists no multicore number should appear in an external document.** Note also `docs/hardware_logs/baremetal_speed_findings_2026-07-29.md:339`: multicore and the A12 clock fix **do not multiply** — decode arithmetic intensity is 5.69 FLOP/byte, so above ~1.42–1.78 GHz the path is bandwidth-bound and the roof on 778.3 MB/token traffic is ~10.3–12.8 tok/s, not 5.58 × 4. | ⚠️ 2.14× commit-prose only / ❌ 8.25 tok/s contradicted | commit 154f00a (prose, no log); retraction basis: commit e659a1f; provenance sweep 2026-07-29 |
| A5 | int8 activations: PPL 12.801→12.738, ~2% cost — quality adopted, speed rejected (no VNNI on Comet Lake) | ✅ | default = ["int8_act"]; bench at aegis-core/benches/ |
| A6 | CTZ sparse kernel REJECTED: real zeros **42.21%** (not 61.8%); AVX2 matvec **6.59×**, batched GEMM **11.74×** faster than CTZ. **⚠️ THIS ROW'S ONLY PROVENANCE WAS A SOURCE FILE, WHICH RULE B DOES NOT ADMIT — re-measured to an instrument log 2026-07-30, and two of its three numbers moved.** (i) `40.8%` came from a 6-tensor *sample*; the full 210-tensor scan (2.084 G weights) gives **42.21%** — the bench's own header said so at `aegis-core/benches/ctz_vs_simd.rs:24-26` while this row and `docs/TECHNICAL_REPORT.md` kept quoting the sample. (ii) **`12.2×` has no parent anywhere** — no log, and the bench header itself said 11.7×; the logged value is **11.74×**. (iii) `6.6×` survives (6.59× logged). The rejection verdict is unchanged and strengthens: CTZ ties the matvec only at ~99% zeros, the GEMM only at ~99.5%, and wins only at literally-all-zeros (0.19×). Clock state bound into the log (TSC nominal 2.1118 GHz, effective/nominal **1.542**) per Rule A's RDTSC corollary. | ❌→✅ (settled negative; figures corrected 2026-07-30) | docs/hardware_logs/ctz_vs_simd_2026-07-29.log:27-32 (instrument, i5-10210U crosvm guest, git_head a249b2c) |
| A7 | LUT-mpGEMM (T-MAC pshufb) REJECTED — **but on memory-traffic grounds; the "16% SLOWER" kernel verdict is RETRACTED.** The bench this row cited never measured the f32 arm it claimed to lose to (its own comment said "this run does not measure that kernel"; the baseline was typed into a comment as "~14-16 GMAC/s ... and 9.86"). Same-binary interleaved A/B, 3 logged runs (2026-07-30, dev box i5-10210U): pshufb-effective **8.24–11.29 GMAC/s** (bimodal, 36% swing) vs f32 matvec **8.94–9.36** (stable) — in the modal state pshufb is ~20% FASTER kernel-side; the ordering flipped in 1 of 3 runs. The rejection stands anyway: 4 bits/weight takes decode traffic 780,142,296 → 1,302,973,872 B/token (**1.670×**, derivation in findings note) on a path that is bandwidth-bound at post-A12 clocks (roof 10.3–12.8 tok/s → ~6.2–7.7), so pshufb's best kernel day (1.21×) nets to **0.72× decode** — and batched GEMM (**20.68–23.67 GMAC/s**) beats it ~2× for prefill in every run. No operating point wins. VNNI-hardware revisit note survives. | ❌→✅ (settled negative; evidence replaced 2026-07-30) | docs/hardware_logs/lut_mpgemm_sameproc_ab_2026-07-30.log, _run2, _run3 (instrument, same-binary) + lut_mpgemm_ab_findings_2026-07-30.md (derivation) |
| A8 | Energy 3.99 J/token incremental multicore (4.63 single-thread), battery-discharge method | ✅ | 2026-07-14 logged runs; supersedes unlogged 3.31 |
| A9 | Full WikiText-2 test set PPL 15.825 (chunked cold-KV, pruned vocab; sample-mode anchor 10.348) | ✅ | docs/hardware_logs/wikitext2_full_ppl_2026-07-12_run.log |
| A10 | Fleet: Dell i5-5200U bare-metal SIMD 4.84× / HP N4020 P-state write 0.17→0.42 tok/s / QEMU matrix 6/8. **⚠️ THE DELL FIGURES IN THIS ROW WERE TAKEN UNDER A PLATFORM THROTTLE — SUPERSEDED BY A12.** Every Dell number published 2026-07-09 → 2026-07-29 was measured at core ratio 5 (~500 MHz, 22% of nominal). The SIMD *ratio* 4.84× survives (both arms were equally clamped); the absolute tok/s do not. | ✅ ratio / ❌ absolutes | docs/hardware_logs/gauntlet_dataset.tsv + per-machine logs |
| A12 | **Dell i5-5200U was clamped to core ratio 5 by an externally-asserted bi-directional PROCHOT, not by thermals (`hot=0`, `temp=Tj-72C`). Clearing MSR_POWER_CTL[0] with read-back, then requesting max P-state, moves it to ratio 25 — reproduced on 3/3 cold boots.** CORRECTED BitNet b1.58-2B-4T decode: **0.62 → 3.03 tok/s** (3,548,301,534 → 726,238,201 ticks/tok, ÷ TSC 2.1975 GHz). ⚠️ that pair is CONFOUNDED — the arms are 2026-07-12 vs 2026-07-29 builds and 8 commits touched aegis-core/src between them, incl. 019cd81 (forward-pass change). **The CLEAN A/B is the 14.17M model, same-day builds differing only by STAGE 7: 53,250,965 → 13,791,915 ticks/tok = 3.861×** (41.3 → 159.3 tok/s), byte-identical 214-token output at both clock speeds. NOT NOVEL — MSR 0x1FC bit 0 is ~15 years of prior art (ThrottleStop; arter97/DisablePROCHOT does it from UEFI with no OS). Report as a **retraction of our own numbers**, never as a speedup: ratio 5→25 is 5.000× by construction. No clear-only arm was run, so "clear before boost" is an untested hypothesis that prior art (erpalma/throttled #163) contradicts. Durability across S3/EC-rewrite untested. | ✅ | docs/hardware_logs/m7_BAREMETAL_bdprochot_FIXED_2026-07-29.log, bitnet_baremetal_postfix_2026-07-29.log, m7_baremetal_prompts_postfix_2026-07-29.log; fix commit f337137 |
| A11 | Peak mem 571,912,597 B working-heap high-water; TOTAL ≈1.35GB with weights — never quote 545MB alone | ✅ | allocator AtomicUsize fetch_max, commit 6f29c6b |

| A13 | **MECH v1 (2026-08-01, Dell i5-5200U bare metal, hands-off boot): Band-3 decomposition — H1 console DEAD, H2 turbo-bin CONFIRMED at +7.36%.** Bit-exactness gate PASS (3 prompts × LOUD/QUIET/QUIET2, responses byte-identical, verified programmatically). H1: preregistered share formula gives 0.02–0.05%, but the MECH loop times `process_intent` at 1.65–1.67G ticks/tok = **119–250× the 07-31 gauntlet decode path** — honest verdict uses absolute console cost (LOUD−QUIET = 0.31–0.84M ticks/tok) ÷ 07-31 gauntlet denominators = **≤6.0% of protocol per-token cost**: cannot produce the +89% per-token gradient or the −39.3% pooled gap. H2: QUIET2/QUIET time ratio **0.9264** on all three prompts vs 25/27 bin prediction 0.9259; TURBO_DIAG cur_ratio 25→27 post MWAIT-C6 AP-park; clock 113%→122% — the entire effect is the 1-core turbo bin. **Adopt AP-PARK in the production boot path.** Gap accounting: residual −34.5% UNEXPLAINED → H3 (UEFI MTRR/PAT audit) next; Band 3 stays unpublishable. Side finding **MECH-A1**: the `process_intent` path is 119–250× slower per token than the harness decode path, same boot — needs its own investigation. Unpreregistered L replicate: decode medians 320.7–367.8 tok/s, consistent with 07-31; sign unchanged. | ✅ decomposition / ❌ H1 dead / ⬜ H3 open | docs/hardware_logs/mech_U_BOOTLOG_2026-08-01.txt + mech_L_BOOTLOG_2026-08-01.txt (instrument); docs/MECH_ANALYSIS_2026-08-01.md (derivations, md5 chain); docs/RUNCARD_MECH_2026-08-01.md (prereg) |
| A14 | **MECH-A1 ROOT CAUSE (2026-08-01): the staged MECH binary was built with the STOCK `x86_64-unknown-uefi` target, whose `+soft-float` feature scalarizes every f32 op — including `#[target_feature(enable="avx2")]` kernel bodies — into soft-float library calls. Census of the staged binary (md5 431ff3a8, 453,120 B, preserved as artifacts/BOOTX64_MECH_softfloat_2026-08-01_431ff3a8.EFI): **xmm=0 ymm=0 vfmadd=0**. Hardfloat rebuild via build_hardfloat.sh (x86_64-uefi-hardfloat.json, features "-mmx", post-H3-merge tree, md5 7ed77474): xmm=6855 ymm=433 vfmadd=210.** MECH-A1 (A13's 119–250× process_intent anomaly) is therefore a BUILD-PROVENANCE failure, not an engine property: the MECH loop ran a zero-SIMD binary. Consequences: (1) H2's turbo-bin ratio 0.9264 is clock arithmetic and survives; H1's ≤6.0% bound was already computed against hardfloat (07-31 gauntlet) denominators and only strengthens on a hardfloat re-boot; (2) MECH-A1 is REMOVED from H3's evidence set; (3) staging discipline: ONLY build_hardfloat.sh output may be staged, gated by scripts/check-efi-simd.sh (objdump census, fails on ymm=0 or vfmadd=0); the stock-target command is demoted to correctness-only in CLAUDE.md. Falsifiable prediction for the next Dell boot: hardfloat MECH re-boot returns process_intent to the band implied by A13's gauntlet denominators (1.65–1.67G ÷ 119–250 ≈ 6.6–14M ticks/tok). | ✅ mechanism (census) / ✅ CONFIRMED on iron 2026-08-01: process_intent 1.65–1.67G → 6.48–7.59M ticks/tok LOUD = 217–258× recovery, inside the predicted band | docs/hardware_logs/mecha1_softfloat_census_2026-08-01_v2.log (instrument; supersedes v1, whose Binary-1 section was mislabeled by a concurrent rebuild) + docs/hardware_logs/mech_U_BOOTLOG_2026-08-01.txt / mech_L_BOOTLOG_2026-08-01.txt (timing attribution) |
| A15 | **ReLU² activation sparsity is real and large at the down_proj input (2026-08-01): BitNet-2B pooled decode mean z=0.7888 exact zeros (per-layer decode means 0.667–0.908, U-shaped in depth; prefill 0.7786 agrees), measured in OUR engine at the exact buffer the down_proj kernel consumes, int8_act ON. SwiGLU control (M7, silu): z=0.2092 — the int8 dead-zone floor — so the relu2-attributable excess is ~0.58 absolute.** Adversarially re-verified: an independent rebuild re-ran both probes and reproduced every per-layer number bit-for-bit. Decision math: down_proj = 25.5% of per-layer ternary elements (132.7 MB of the 522.8 MB per-token weight stream); column-skip at z=0.7888 → 4.73× less down_proj work, ~20.1% layer traffic saved (13.4% incl. LM-head read); clears even the eps=1.0 pessimistic break-even (z=0.50) with 2.4× headroom → column-skip kernel prototype JUSTIFIED. Caveats: 27 decode tokens only (4 prompts EOS early); z counts −0.0 as zero (right for MAC/traffic math; a bit-exact skip kernel must special-case −0.0 columns); prefill/batched benefit unmeasured. DISTINCT from settled A6 (static weight zeros, per-element gather): dynamic per-token activation zeros, contiguous 640-byte column granularity, no gather, no bit-scan. Machine: i5-10210U crosvm guest — COUNTS ONLY, no timing. | ✅ (counts, reproduced) / ⬜ column-skip A/B pending | docs/hardware_logs/relu2_act_sparsity_bitnet2b_2026-08-01.log (instrument; per-layer table + config echo) |
| A16 | **Fused dual/tri matvec SETTLED NEGATIVE (2026-08-01, Dell i5-5200U, minimal-Linux initramfs, idle, 3 interleaved captures): fused/sequential best-of time ratios — DUAL BitNet-2B SwiGLU 1.611–1.733, TRI GQA 1.434–1.442, DUAL M7 1.734–1.735, TRI M7 1.912–1.914. Fused is 1.43–1.91× SLOWER on every shape, every capture; outputs byte-identical throughout (gates pre+post).** Mechanism as predicted from disassembly: M×4 row pointers exceed 16 GPRs → inner-loop stack reloads outweigh shared-input-load amortization. CLOSES the recurring "dual matvec" proposal with a measured answer — do not re-propose without a register-budget-aware redesign. Clock-state blocks in-log (eff/nom 0.999–1.222; ratios stable across clock states, which is what same-binary interleave is for). | ❌ settled negative | docs/hardware_logs/kernelab_L_BOOTLOG_2026-08-01.txt (instrument, 3 captures) + docs/MECH11_ANALYSIS_2026-08-01.md (derivation) |
| A17 | **Bitplane-dense matvec SETTLED NEGATIVE (2026-08-01, same L-stick boot, 3 interleaved captures, 2560×6912): incumbent LUT+FMA 7.47 GMAC/s (identical all 3 captures); bitplane (i) ordered 0.39–0.40×, (ii) dual-acc 0.41× incumbent.** Armchair prior 0.85–1.0× falsified downward: vpshufb/vpand/vpcmpeqd mask expansion per 8-column group costs ~2.5× the 256×4-f32 LUT unpack. Byte-identity gates held pre- and post-timing in every capture ((i) == incumbent bit-for-bit; (ii) == its scalar mirror). Surviving virtue is CIS-1-shaped, not performance: the scalar mirror is bit-identical to the AVX2 kernel across ISAs. NOT A6 (no bit-scan), NOT A7 (2 bits/weight, same traffic) — a third, now-settled family. pack_bitplanes one-time cost ~109–133 ms/tensor. | ❌ settled negative | docs/hardware_logs/kernelab_L_BOOTLOG_2026-08-01.txt (instrument) + docs/MECH11_ANALYSIS_2026-08-01.md (derivation) |
| A18 | **MECH v1.1 (2026-08-01, Dell, hardfloat 322efa42, one hands-off boot): P1–P5 all landed.** H1 console share on the honest denominator: LOUD−QUIET = 332,269 / 301,815 / 874,052 ticks/tok = **5.1% / 4.2% / 11.5%** of LOUD (same absolute band as v1 — console cost is decode-speed-independent; buffering is worth 4–12%). H2 replicated: QUIET2/QUIET = 0.93078–0.93177 (bin arithmetic 0.92593; the ~0.6-pt excess = non-clock-scaling memory share), cur_ratio 25→27, clock 113%→122%. **H3 FALSIFIED: first bare-iron MTRR/PAT dump — every engine range (image, MODEL.SAF, EMBED.BIN, VOCAB.BIN, heap0, heap1) is WB (uniform)**; PAT power-on default; Band-3 residual is NOT memory attributes → needs H4. **P5 double pass: LOUD==QUIET==QUIET2 byte-identical, AND soft-float v1 == hardfloat v1.1 responses byte-identical on all 3 prompts** (fmaf libcalls preserved fused rounding) — strongest cross-implementation determinism exhibit; direct CIS-1 support. Unpreregistered indicators (flagged): unikernel QUIET2 384.1/340.6/351.0 tok/s vs Linux L3 replicate 364.4/287.5/282.2 decode-only (+5.4%/+18.5%/+24.4%, arms not protocol-paired — Band-3 sign does not survive v1.1 conditions; preregistered MECH v2 needed to publish); batched prefill GEMM on iron 1.5605× (102,570,215→65,728,001 ticks, 27 tok, clock-matched); SIMD prefill ratio 6.38× at matched clock; A12 durability: bd-PROCHOT clear survived a power cycle (boot-pre bdprochot_en=0, no write issued). | ✅ H1/H2 measured, H3 falsified, P5 double-pass / ⬜ H4 open | docs/hardware_logs/mech11_U_BOOTLOG_2026-08-01.txt + kernelab_L_BOOTLOG_2026-08-01.txt (instrument) + docs/MECH11_ANALYSIS_2026-08-01.md (derivations) |
| A19 | **CIS-1 E2 GATE PASSED (2026-08-01): full-integer-dominant forward path costs +0.3127% perplexity on M7 — 16× inside the preregistered +5% kill line.** Teacher-forced, pinned 471-token heldout, same run/same binary/same token window: float PPL 5.639491 (reproduces the m7_final_roundtrip anchor 5.639), integer PPL 5.657126. Deterministic: 3 independent runs bit-identical (argmax FNV-1a-64 digest 0x42E820C2A8A59CD6); reproduced again from the merged main tree. Integer stages: BF16→Q.20 embeddings (exact RNE on bit patterns), i64 Q.20 residual, fused integer rmsnorm+absmax-i8 quant, all 7 ternary matvecs/layer (cis::ternary_matvec_i8), LM head, argmax. DECLARED HYBRID (still f32): attention core (RoPE/softmax/V-mix) + silu elementwise — cross-kernel-path bit-identity NOT claimed until SOFTMAX-I/ROPE-I/ACT-I normative constants land. RNE arbitrated as the single normative rounding mode (whole-path cost measured, not assumed). Goldens (12 new) from an independent Python big-int generator, checked in (scripts/cis_e2_golden_gen.py). Machine: i5-10210U crosvm — QUALITY numbers only, no timing. Caveat: vocab 8192 full-source; cross-tokenizer PPLs not comparable. Next: BitNet-2B leg; integer reference kernel is ~3.5× slower than float in this unoptimized scalar form (incidental observation, not a measurement). | ✅ gate passed / ⬜ BitNet-2B leg + integer attention open | docs/hardware_logs/cis1_e2_int_vs_float_ppl_m7_i5-10210U_crosvm_2026-08-01.log (instrument, artifact sha256s + both transcripts) + docs/CIS-1_SPEC_DRAFT_v0.1.md v0.2 notes (derivation/spec) |
| A20 | **CIS-1 v0.3: FULL-INTEGER forward pass (2026-08-01) — attention and activations included — costs +0.0637% perplexity vs float on M7 (5.643085 vs 5.639491; hybrid was +0.3127%): the all-integer path is CLOSER to float than the hybrid, because dropping the two f32→fixed re-entry quantizations outweighs the Q0.15/Q1.30 table quantization it adds.** 3 runs bit-identical incl. a fresh-CARGO_TARGET_DIR rebuild (argmax digest 0xBED4A17A1A5EE296); hybrid path untouched (A19 digest reproduced exactly). Normative integer constants (exp LUT digest 0x66C2A0EEB8C2DC43, RoPE table 0xD8345EBF01E990FA) generated AND cross-checked by the independent Python big-int generator; softmax sum-to-one |Σp−2^15|≤⌈T/2⌉ and monotonicity hard-asserted; numerical-safety bounds derived in spec v0.3 for hidden 384–2560, head_dim ≤128, seq ≤512. **Cross-kernel-path bit-identity now holds BY CONSTRUCTION** (FullInt is pure scalar integer Rust — no SIMD dispatch, no float kernels entered); cross-MACHINE replay is the E4 exhibit, pending. Machine: i5-10210U crosvm — QUALITY only. Open: BitNet-2B FullInt needs KV-cache footprint work (~630MB at 4096 pos); spec v0.3 ratification before v1.0. | ✅ gate passed (78× headroom) / ⬜ E4 cross-machine replay pending | docs/hardware_logs/cis1_fullint_attention_ppl_m7_i5-10210U_crosvm_2026-08-01.log (instrument) + docs/CIS-1_SPEC_DRAFT_v0.1.md v0.3 notes (derivations) + scripts/cis_e2_golden_gen.py (independent generator) |
| A21 | **CIS-1 E2, BitNet-2B leg (2026-08-01): the production-scale brain passes the integer gate at +0.7408%** — integer PPL 30.934140 vs float 30.706665, teacher-forced, 200-token wikitext-2 test window, same run/binary/window; 6.7× inside the +5% kill line. Two runs bit-identical (argmax digest 0x24C4E510A86659D6). Enablement (E2-7): on-the-fly Q.20 embedding conversion (no ~1 GB resident table on the 6 GB box), per-vector block exponent at the hybrid boundary (G≤F, exact; provably degenerates to plain Q.20 at M7 ranges — M7 digests unchanged after merge, re-verified), headroom recalibration for hidden 2560/inter 6912 (normq n≤8192, residual <2^50, derivations in spec E2-7 notes). Caveats: pruned-vocab model (50,256/128,256, ASCII-oriented) — never compare this PPL cross-tokenizer; 200-token window on an <unk>-dense region — NOT comparable to the 1,898-token 10.758 anchor; the same-run float is the base. Machine: i5-10210U crosvm — QUALITY only. With A19 (M7 hybrid +0.31%) and A20 (M7 full-int +0.064%): every E2 leg named in the preregistration has now passed. Open: BitNet FullInt (KV footprint), E4 cross-machine replay. | ✅ gate passed / ⬜ BitNet full-int open | docs/hardware_logs/cis1_e2_bitnet2b_int_vs_float_ppl_i5-10210U_crosvm_2026-08-01.log (instrument, sha256s + both transcripts) + docs/CIS-1_SPEC_DRAFT_v0.1.md E2-7 notes (derivations) |
| A22 | **MECH v2 PREREGISTERED PAIRED REDO — DECISION RULE MET (2026-08-01, Dell i5-5200U, two hands-off boots): the ring-0 unikernel beats minimal Linux on 3/3 per-prompt decode medians, n=10 per prompt per arm.** U (QUIET2 conditions, post AP-PARK, console buffered) medians 386.4 / 342.4 / 352.7 tok/s (ticks ÷ TSC 2.1975 GHz, prefill amortized — conservative against U) vs L (tmpfs-buffered console, performance governor, bd-PROCHOT clear) decode-only medians 372.6 / 310.1 / 334.7 → throughput form **+3.7% / +10.4% / +5.4%**; the preregistered form Δ=(t_L−t_U)/t_L in s/token gives **+3.6% / +9.4% / +5.1%** — 3/3 under either metric (correction 2026-08-01: both forms stated after fact-check). P-V2-3 PASS: 27/27 within-boot EXACT + responses byte-identical to v1.1 QUIET2 (third binary, third boot). **P-V2-2 FAIL (reported): spread 4.7–9.6% vs predicted <3%** — bit-exactness perfect, wall-time wobblier than claimed. Declared caveats carried: different code paths (process_intent vs harness), EOS ±1, timing boundaries per prereg §Metrics. Band-3 (A13) sign is now overturned under matched conditions by a preregistered design. | ✅ 3/3 + exactness (publishable per prereg) / ❌ P-V2-2 | docs/hardware_logs/mech2_PREREGISTRATION_2026-08-01.md (prereg, committed pre-boot) + mech2_U_BOOTLOG_2026-08-01.txt + mech2colskip_L_dell_BOOTLOG_2026-08-01.txt (instrument; _dell2_ replicate) |
| A23 | **Column-skip WINS — first kernel-candidate victory (2026-08-01, Dell i5-5200U, minimal-Linux initramfs, idle): 2.88–2.89× vs incumbent on the REAL BitNet-2B down_proj activation distribution** (94 of the 750 captured vectors loaded by the bench, subset mean z=0.7927 — slightly sparser than the 750-vector pooled 0.7886, i.e. mildly favorable; correction 2026-08-01 after fact-check vs the in-log REAL-vectors header), ordered variant — **byte-identical to the incumbent by construction and test** → zero quality risk on adoption. Chain variant 2.80×. Replicated on a second cold boot, 3 interleaved captures per boot, pre+post identity gates held on every timed vector. Honest curve: z=0 overhead 0.69× (skip machinery ≈45% when nothing skips; eps≈0.45 vs A15's pessimistic 1.0), synthetic z=0.9 → 5.9×. GMAC/s stated NOMINAL (skipped work counted as done; speedup = time ratio). NOT wired end-to-end — kernel-level claim only; adoption needs inference.rs wiring + per-layer repack + a system A/B. | ✅ kernel-level (replicated) / ⬜ end-to-end adoption open | docs/hardware_logs/mech2colskip_L_dell_BOOTLOG_2026-08-01.txt + _dell2_ (instrument) + relu2_act_capture_bitnet2b_2026-08-01.log (input provenance) + artifacts/relu2_down_in_bitnet2b_2026-08-01.av1 (the vectors) |
| A24 | **FABLE-0 gate G1 measured (2026-08-01, Dell i5-5200U, minimal-Linux initramfs, two boots): peak sequential read 11.19 / 10.95 GB/s (1 thread), 11.63 / 11.70 GB/s (4 threads); ternary weight-stream pattern 0.62 GB/s (both boots, 1 thread)** — the stream figure is a SCALAR LUT-walk LOWER BOUND on the engine's streaming rate (the bench's own caveat), not the engine rate; its 18× gap to peak sequential quantifies how far the 2-bit unpack pattern sits from the machine ceiling and feeds the FABLE-0 bandwidth-vs-compute budget. Clock-state blocks in-log. | ✅ measured (2 boots) | docs/hardware_logs/mech2colskip_L_dell_BOOTLOG_2026-08-01.txt + _dell2_ (instrument) |
| A25 | **CIS-1 FOUR-IMPLEMENTATION DIGEST JURY COMPLETE (2026-08-02): `CIS_SELFTEST digest=76985613c965f643 ALL_PASS=true` on HP Stream Celeron N4020 (Gemini Lake, SSE2-class, bare-iron minimal-Linux) — identical to Dell i5-5200U bare iron (2 cold boots), crosvm dev host (i5-10210U), and QEMU/TCG emulation. Four distinct microarchitectures + codegen paths, one bit-exact integer semantics. Correctness leg only; no performance numbers taken from it.** | ✅ measured | docs/hardware_logs/hp_L_BOOTLOG_2026-08-02.txt |
| A26 | **CIS-1's throughput cost MEASURED for the first time (2026-08-05), and it is a property of the microarchitecture, not of CIS-1.** Two machines, one binary, three interleaved captures x three shapes each, 9/9 bit-exact and 0 void on both. **Dell i5-5200U (Broadwell-U): C/B median 1.248x** (range 1.239–1.260) — integer semantics cost 25% against *scalar* float. **HP N4020 (Gemini Lake): C/B median 0.961x** (range 0.856–0.963) — integer is 4–14% **FASTER**. Broadwell's two FMA units favour the float LUT+FMA path; on Goldmont Plus, where FP is weak, integer wins. **This retires both prior claims about CIS-1's cost:** "near-zero overhead" had no log and is unsupported; the "5.9–6.6x slower" reading — quoted from wall-times whose own log disclaims them on line 3 — is refuted as a *determinism* cost, since the Dell decomposes it as **4.61x absent SIMD x 1.248x semantics**. METHODOLOGY CONTROL: the HP has no AVX2, so arm A must fall back onto arm B; **A/B measured 1.000x (0.999–1.001, n=9)** on a machine where the true answer is known in advance, which is what licenses the Dell's A/B = 0.217x. Clock state bound to every capture (HP effective/nominal 2.505x — ratios unaffected, absolute tick figures are not). ⚠️ A dev-host crosvm smoke of the same binary read C/B = 0.745x and was **reversed by iron**; recorded as a caught Rule-A near-miss. Kernel ratios are NOT token ratios; end-to-end decode and cis_attn remain unmeasured. | ✅ measured, 2 machines | docs/hardware_logs/cis_vs_float_L_dell_BOOTLOG_2026-08-05.txt + cis_vs_float_L_hp_BOOTLOG_2026-08-05.txt (instrument, 3 captures each) |
| A27 | **The AVX2 integer kernel BEATS the float AVX2 incumbent 2.9x — and the order control that licenses the number (2026-08-06, Dell i5-5200U, bare metal, bd-prochot cleared, 3 captures x 3 shapes).** `cis_avx2::ternary_matvec_i8_avx2` (vpsignb-as-ternary-multiply, same 2-bit packing/traffic as the incumbent — NOT the A7 LUT-mpGEMM) measured **D/A median 0.340x** (range 0.331–0.439): the bit-exact integer path is **2.94x FASTER** than the hand-written float AVX2 kernel at parity SIMD. **D bit-identical to the scalar reference 9/9, 0 void** — determinism is not merely free on this microarchitecture, it wins. D/C 0.061x = 16.4x over the scalar reference; C/B 1.239x replicates A26's Dell figure a third boot running (1.248/1.250/1.239). METHOD NOTE, recorded because it is the finding's provenance: the FIRST arm-D run gave D/A 0.276x but was REJECTED — arm A ran +58% across runs where clock arithmetic predicted +28% while B and C tracked the clock to 0.1%, i.e. arm D perturbed A's cache state and flattered the ratio. The bench then gained an A' re-measure of arm A after D each rep; on this run **A'/A median = 1.000x** (D/A = D/A' to three decimals), so the 0.340x stands as measured, not assumed. Kernel contract hardened same-day after an adversarial sweep: preconditions shared with the reference (rejection surface must not depend on ISA), simd_on() dispatch, target_feature on the nested step; 14 tests incl. exhaustive mechanism proofs (262,144 bit-pair cases; vpsignb 1,024 cases with the -128 wrap pinned both ways). NOT established: token-level throughput (kernel ratios are not token ratios), cis_attn, any non-Dell arm-D figure. | ✅ measured, order-controlled | docs/hardware_logs/cis_avx2_armD_ordercontrol_L_dell_BOOTLOG_2026-08-06.txt (instrument, 3 captures) + cis_avx2_armD_L_dell_BOOTLOG_2026-08-05.txt (rejected first run, kept as the confound record) |
| A28 | **CIS-1 CROSSES ITS FIRST ISA BOUNDARY (2026-08-07): `CIS_SELFTEST digest=76985613c965f643 ALL_PASS=true` on aarch64** — GitHub `ubuntu-24.04-arm` hosted runner, `uname -m`=aarch64 asserted in-job, CPU part 0xd49 (Neoverse N2 class), Azure southcentralus. Identical to all five x86 legs (A25 + subsequent boots). The portability claim upgrades from "four x86 codegen paths" to **two ISAs**: the reference semantics compiled untouched (`cis.rs` has zero imports; lib.rs now cfg-gates every x86-intrinsic module) and reproduced the digest on first execution. CI gate `arm-digest.yml` now pins this on every push — a future mismatch fails the build with instructions to preserve the log as a finding, not to fix it into silence. Cloud runner, so the machine is named as precisely as the platform allows; identity artifact only, no timing quoted or quotable (Rule A). | ✅ measured (CI instrument) | docs/hardware_logs/cis_selftest_aarch64_github_arm_2026-08-07.log (full run log) + .github/workflows/arm-digest.yml (the standing gate) |
| A29 | **TOKEN-LEVEL CROSS-ISA IDENTITY — Objective 1, first leg (2026-08-07): a complete greedy decode is bit-identical across ISAs.** `cis_decode` runs the FULL pipeline — tokenizer, embeddings, all 7 layers, integer attention, integer LM head, `argmax_i64` — on the tracked M7 model (hidden 384, 8K vocab) and prints one digest over every token id, prompt included: `CIS_DECODE digest=67e8c0a96abc04e1 prompt_toks=4 gen_toks=64 mode=fullint`. **Reproduced identically on x86_64 and aarch64** (GitHub `ubuntu-24.04` + `ubuntu-24.04-arm`, both jobs green in one run), deterministic 3/3 on the dev host, output is coherent TinyStories text ("…there was a little girl named Lily…") — the full-integer path decodes fluently, not just identically. Both digests are now standing CI gates (`arm-digest.yml`): the x86 job pins the tree, the ARM job pins the ISA, so a future mismatch is attributable on sight. Design choices inside the pin: greedy only, EOS ignored (fixed 64 tokens, no stop-condition dependence), `CisMode::FullInt` only — the Hybrid arms are now explicitly x86-only because their f32 legs carry no cross-ISA claim. **NOT established: BitNet-2B scale** (weights not in repo, 2B decode does not fit CI) — that leg needs a local ARM board or a paid runner and stays open. Identity artifact only; no timing quoted or quotable (Rule A). | ✅ measured (CI instrument, both ISAs) | docs/hardware_logs/cis_decode_token_crossisa_ci_2026-08-07.log (full dual-job run log) + .github/workflows/arm-digest.yml (standing gate) |

| A30 | **NEON CIS-1 KERNEL BIT-IDENTICAL ON REAL ARM SILICON (2026-08-07) — Objective 2, first leg.** `cis_neon::ternary_matvec_i8_neon` — `vmulq_s8` as the ternary multiply (ARM has the real i8 multiply `vpsignb` substitutes for), per-byte bit-pair extraction through a `vqtbl1q_s8` code LUT, exact `vpaddlq/vpadalq` widening, the shared `check_tmv_preconditions`, and the same `-128`→scalar-fallback contract as `cis_avx2` (`vmulq_s8` wraps `-128 × -1` identically to `vpsignb`; the wrap is pinned in both directions by test). Proven on the GitHub `ubuntu-24.04-arm` Neoverse runner, all three suites green in one job: equivalence 5/5 (bit-identity vs the reference incl. tails, code `11`, extremes, row independence), contract 6/6 (rejection surface identical to the reference either side of the block threshold), mechanism 3/3 (exhaustive: every byte × 4 pair positions through the LUT; all 256 activations × 4 codes through `vmulq_s8`; widening-chain extremes). Both standing digest pins (A28 op-level, A29 token-level) held in the same run. Wired into `arm-digest.yml` as a standing gate on every push. **No ARM performance number exists or is claimed** — Rule A requires named physical hardware and this runner is shared cloud; the throughput leg of Objective 2 stays open until an ARM board is on the bench. Incidental finding, same push: `cargo test --test X` auto-builds all bin targets, so the seven x86-only bench bins needed non-x86 stub mains before any NEON test could run — first neon-dev CI run preserved red in the run history as the record. | ✅ measured (CI instrument) | docs/hardware_logs/cis_neon_tests_aarch64_github_arm_2026-08-07.log (full aarch64 job log: uname assert, 6+5+3 test results, both digest lines) + .github/workflows/arm-digest.yml (standing gate) |

| A31 | **THE SPEC PASSES ITS OWN TEST: TWO CLEAN-ROOM IMPLEMENTATIONS REPRODUCE THE TIER-2 DIGEST (2026-08-08).** Two independent implementers (subagents, different personas) were given the v1.0.1 spec text as their sole source — reference source access forbidden and not used — and each wrote a from-scratch scalar Rust implementation of the §5 ops (400 and 484 lines, verified distinct by diff/md5). Both, linked against the verbatim-ported public conformance harness, print exactly `CIS_SELFTEST digest=76985613c965f643 ALL_PASS=true` — all 14 sections, first run after their own cargo-check. This is the artifact A30 could not provide: the spec is now known implementable-from-text, and specifically the v1.0.1 §5.4 erratum (RMSNORM-I's Q2.30/2¹⁵ grids) is what made it possible — both implementers hit RMSNORM-I's golden on first attempt from the corrected prose. **Honest scope:** the public conformance suite's golden vectors were visible to the implementers, and both reports state the A8 goldens were needed to settle `from_ratio` edge cases the text leaves open — so the proven claim is "implementable from spec text + public conformance suite, without reference source," not from prose alone. The implementers' ambiguity reports converge on the same spec gaps (queued as v1.0.2 errata): `from_ratio`'s rejection surface and S-selection bracket are implicit; its RNE-to-2³¹ renormalization at S>0 is unstated; QUANT-ACT/RMSNORM-I slice-length preconditions, empty-ARGMAX, and `rne_div` den≤0 behavior are unspecified. Identity artifact only (dev host i5-10210U/crosvm named; no perf figure exists or is claimed). | ✅ measured (instrument) | docs/hardware_logs/cis_cleanroom_tier2_2026-08-08.log (both runs, md5s, independence check) |
| A32 | **THE WITNESS CROSSES THE ISA BOUNDARY (2026-08-08): an x86-minted decode receipt verifies bit-for-bit on aarch64, in public CI.** The golden receipt `tests/golden/witness_v1_m7_once64.receipt` — minted on the crosvm dev host (i5-10210U) and carrying SHA-256 bindings of all three model artifacts, the 64 generated token ids, the token digest `67e8c0a96abc04e1`, and a chained SHA-256 commitment over every decode step's full i64 logit vector — was replayed from source on a GitHub `ubuntu-24.04-arm` hosted runner (public run 31249589879, snapshot ce93bbb): `artifacts 3/3`, token digest and chain `aee25b770bd7b22e…` reproduced exactly, `VERIFY PASS — replay reproduced 64 tokens, the token digest, and the full logit chain bit-for-bit`. The same run's x86-64 job verifies the identical receipt, and `arm-digest.yml` now pins both legs on every push. This upgrades A28's cross-ISA claim from the op-level selftest digest to a **full decode trajectory: 64 steps × the complete logit vector, hash-chained** — not a summary digest but the entire integer state sequence. Cloud runner, machine named as precisely as the platform allows; identity artifact only, no timing quoted or quotable (Rule A). | ✅ measured (CI instrument) | docs/hardware_logs/witness_receipt_aarch64_ci_2026-08-08.log (full run log, both jobs) + .github/workflows/arm-digest.yml + tests/golden/witness_v1_m7_once64.receipt |
| A33 | **FIRST PHYSICAL-IRON KIT VERIFICATION (2026-08-08): the Provable AI Kit stick boots on the Dell Inspiron 15 (i5-5200U, Broadwell-U) with no OS present and re-derives the golden receipt bit-for-bit.** Operator physically present; F12 boot, Secure Boot off. BOOTLOG.TXT appended by firmware: `STAGE V: witness verify PASS — VERIFY PASS — this machine reproduced all 64 decode steps' full logit vectors bit-for-bit, with no OS underneath`. Receipt on stick md5-identical to `tests/golden/witness_v1_m7_once64.receipt` (87c45bdd…); payload = the QEMU-proven aegis-kit-iron.img (320e1918…), stick readback verified bit-identical before boot. Attribution of the new BOOTLOG entry to the Dell: appended after the baked-in QEMU entry, with a different firmware memory map (EMBED.BIN @0x3AC000 vs QEMU 0x1780000); limitation: verifier mode prints no CPUID — see provenance note. Completes the chain crosvm-mint → QEMU → public CI x86-64 → public CI aarch64 (A32) → **physical iron, ring 0**. Identity claim only; no timing (Rule A). | ✅ measured (iron instrument) | docs/hardware_logs/dell_i5-5200U_kit_iron_verify_bootlog_2026-08-08.txt (raw, md5 becd7cef) + docs/hardware_logs/dell_i5-5200U_kit_iron_verify_provenance_2026-08-08.md |
| A34 | **HP LEG (2026-08-08): first-ever unikernel boot on the HP Stream (Celeron N4020, Gemini Lake) — same stick, same receipt, VERIFY PASS.** Operator physically present; payload unchanged since the Dell leg. BOOTLOG.TXT gained exactly one new entry (diff vs banked Dell-era log): `VERIFY PASS — this machine reproduced all 64 decode steps' full logit vectors bit-for-bit, with no OS underneath`. Scalar-path corollary: N4020 lacks AVX2, so a PASS implies the SSE2 scalar path executed (AVX2 would #UD) — the golden receipt has now re-derived bit-for-bit through two disjoint kernel code paths on iron. Attribution limitation stated plainly: the new entry's firmware memory map is identical to the Dell entry's, so in-log evidence does not discriminate the two boxes; attribution rests on operator witness (see provenance note). Also a firmware finding: N4020 UEFI boots the unikernel at all (previously unknown). Identity claim only; no timing (Rule A). | ✅ measured (iron instrument; attribution per provenance note) | docs/hardware_logs/hp_n4020_kit_iron_verify_bootlog_2026-08-08.txt (raw, md5 4fb5fc8b) + docs/hardware_logs/hp_n4020_kit_iron_verify_provenance_2026-08-08.md |
| A35 | **CIS-1 FULL-INTEGER PASSES ON BitNet-2B (2026-08-27): +0.1239% perplexity vs float — 40× inside the preregistered +5% kill line, and CLOSER to float than the hybrid path (+0.7408%, which reproduced A21 exactly as the same-run control).** Teacher-forced, 199 scored tokens, same artifacts as A21 (sha-identical: model 53586af0…, embed e32b99a2…, vocab 5bde1b03…, test.txt d790b833…), dev host i5-10210U crosvm, QUALITY ONLY (Rule A). Two sequential runs identical on every computed value: float 30.706665 / hybrid 30.934140 / full-int 30.744724; argmax digests hybrid 0x24C4E510A86659D6, full-int 0xB274DE03F5862DB7. **The unfixed binary panicked 2/2 runs** (`normq: residual out of range`, cis_infer.rs:313): an env-gated trace (branch cm/e1b-normq-trace) showed attention re-entry exact at ≤38 bits on all 30 layers while the FullInt ACT-I (relu²) MLP output landed on Q.20 at 52–55 bits — a SPEC GAP (§5.10 lacked §5.6's block exponent; the hybrid boundary already had it), not activation growth. Fix (merged, d41ca4a): per-vector block exponent G ≤ F on the ACT-I output (`fix_q_vec`, shift = bits−49, RNE), threaded to ffn_sub; degenerates to G = F at M7 ranges — **CIS_SELFTEST 76985613c965f643 and CIS_DECODE 67e8c0a96abc04e1 unchanged.** Ratified as spec erratum v1.0.3 §5.10. Closes spec §9 "2B full-integer quality not yet measured". Caveats as A21: pruned vocab (50,256/128,256), 200-token window, not cross-comparable; no timing. | ✅ measured (2 runs) | docs/hardware_logs/cis1_fullint_ppl_bitnet2b_i5-10210U_crosvm_2026-08-27_e1c_fixed.log (PASS, instrument) + …_2026-08-27_r2.log (panic, 2/2) |
| A36 | **FIRST TOKEN-LEVEL FULL-INTEGER DECODE DIGEST FOR BitNet-2B, x86-64 leg (2026-08-27): `CIS_DECODE digest=cab11400d737ac4a prompt_toks=4 gen_toks=64 mode=fullint`, identical on two sequential runs, output coherent English ("…in a small town called Greenfield, there lived a young girl named Lily…").** Artifact: `aegis_pruned_model.cis.safetensors` (sha256 facb3597…) = the A21/A35 model (53586af0…) with `__metadata__.aegis_config` added by `aegis-forge/add_aegis_config.py` (values derived from tensor shapes, cross-checked against aegis_pruned_config.json and EMBED/VOCAB sizes; tensor data blob byte-identical). Equivalence proven in the same log: `aegis-eval --cis-full` on the new artifact reproduces float 30.706665 / hybrid 30.934140 (0x24C4E510A86659D6) / full-int 30.744724 (0xB274DE03F5862DB7) exactly. Identity evidence only (dev host i5-10210U crosvm; no timing). This is the x86 anchor for the 2B cross-ISA leg (aarch64 phone / CI runner) — the 2B counterpart of A29. | ✅ measured (identity, 2 runs) | docs/hardware_logs/cis_decode_bitnet2b_fullint_x86_i5-10210U_crosvm_2026-08-27.log |
| A37 | **A THIRD, STANDALONE IMPLEMENTATION VERIFIES THE RECEIPT WITHOUT THE ENGINE (2026-08-27): the `cis-verify` crate — zero external runtime dependencies, no `aegis-core` dependency, `no_std`+alloc core, zero `unsafe`, ~4,700 lines transcribed from the spec with the reference cited per function — reproduces the Tier-2 digest `CIS_SELFTEST digest=76985613c965f643 ALL_PASS=true`, both pinned table digests (exp LUT 0x66C2A0EEB8C2DC43, RoPE 0xD8345EBF01E990FA), the Tier-3 decode digest `CIS_DECODE digest=67e8c0a96abc04e1 … mode=fullint` on first attempt, and prints `VERIFY PASS` on the golden receipt `tests/golden/witness_v1_m7_once64.receipt` (all six checks: parse, 3 artifact hashes, prompt tokenization, 64-step token sequence, cis-digest, witness chain) in ~1.4 s; tamper tests fail naming the field (token-id, chain, model/vocab hash, receipt-parse).** Built by a sonnet agent in three phases (commits 707a769, 446f1f9, e46b637 on cm/cis-verify; merged). Honest scope: a transcription by an LM agent with the reference source visible (unlike A31's clean-room test), so this is evidence that the spec+reference are re-implementable without the engine's SIMD/dispatch code, not an independent third-party audit. Identity evidence only (dev host; the 1.4 s is a convenience figure, not a benchmark). | ✅ measured (identity; tests + CLI + tamper) | docs/hardware_logs/cis_verify_standalone_tier2_tier3_golden_i5-10210U_crosvm_2026-08-27.log |
| A38 | **THE STANDALONE VERIFIER CROSSES THE ISA BOUNDARY IN PUBLIC CI (2026-08-27): `cis-verify` (A37) runs on the GitHub `ubuntu-24.04-arm` runner (`uname -m`=aarch64 asserted in-job) and prints `CIS_SELFTEST digest=76985613c965f643 ALL_PASS=true` (its own transcription of the §5 ops), passes all its suites (81 unit + integration), and its CLI prints `VERIFY PASS` on the x86-minted golden receipt `tests/golden/witness_v1_m7_once64.receipt` — `standalone cis-verify CLI reproduces VERIFY PASS on aarch64.`** The x86_64 job in the same run prints the identical lines. Both are now standing CI gates (`arm-digest.yml`, steps 'cis-verify (standalone verifier) — …' on both jobs): a future divergence between the spec-transcribed verifier and the reference fails the build on either ISA. Upgrades A37 from 'third implementation on the dev host' to 'third implementation, two ISAs, verifying a receipt minted by the first'. Cloud runners: identity artifact only, no timing quoted or quotable (Rule A). | ✅ measured (CI instrument, run 33130091795) | docs/hardware_logs/cis_verify_aarch64_github_arm_ci_2026-08-27.log |

## B. Ternary portfolio (hybrid-engine branch)

| # | Claim | Verdict | Provenance |
|---|-------|---------|------------|
| B1 | Dense→ternary PTQ transmutation is DEAD sub-7B (15 months, zero coherent outputs; field concurs) | ✅ | 103-agent research sweep, ledger §D2; PT2-LLM numbers |
| B2 | Engine generalized: SwiGLU + optional SubLN + untied LM head + config-in-safetensors, BitNet anchor 10.348 reproduced EXACTLY post-change (1899 tokens, --sample mode) | ✅ | docs/hardware_logs/wikitext2_sample1900_2026-07-16_rerun.log |
| B3 | Falcon-E-1B-Instruct runs coherently in the same engine (4.39 tok/s decode 1t re-run; commit msg said ~4.8), self-stops cleanly | ✅ | docs/hardware_logs/falcon_e_1b_generation_2026-07-16_rerun.log |
| B4 | weight_scale convention bug found+fixed: BitLinear divides, engine multiplies — repacker writes reciprocal | ✅ | commit 6e309f9 |
| B5 | hf1bitllm packing axis is [out/4, in] dim-0 — original repack assumption would have silently scrambled Llama3-8B | ✅ | commit c6ce416 review fix |
| B6 | **T2d PASSES: engine tokenization exactly matches Falcon-E's reference tokenizer** (2410/2410 ids, 7325-char sample) after whitespace-run pre-tokenization + id-0 merge-guard fixes; was 2425 vs 2410 (15 extra tokens, every site a broken ' \n \n' group) | ✅ | aegis-core/tests/reference_parity.rs t2d + tests/fixtures/falcon-e-1b/; divergence diagnosis in loop session 2026-07-16 |
| B7 | BitNet T0a anchor RE-PINNED 10.348 → 10.758 (1898 tokens/7401 chars, denser tokenization). Per-token PPL rise is a metric artifact of merging trivially-predictable newline tokens — cross-tokenizer PPLs are not comparable; pin = regression tripwire, not a quality claim | ✅ | docs/hardware_logs/wikitext2_sample1900_2026-07-16_repin.log |
| B8 | **G4a PASSED 2026-07-17: engine 4.837 vs transformers reference 4.831 on identical 1896 ids (t2d_sample prefix) = 0.12% diff** (gate 5%). Falcon-E PPL claims unblocked. The "needs Colab" premise was false — reference ran locally at 4.86 tok/s teacher-forced, 1.28GB RSS | ✅ | docs/hardware_logs/g4a_falcon_e_1b_parity_2026-07-17.log |
| B9 | **BitNet full-testset WT2 PPL, new byte-level tokenizer: 16.124 (312,119 tokens, 1,254,250 chars, ~4.02 c/t), finished 2026-07-19 morning after ~35h** under unit aegis-ppl-rerun. NEW ANCHOR PIN. NOT comparable to the old-tokenizer full-set 15.825 (see B7: denser tokenization inflates per-token PPL; cross-tokenizer PPLs are metric artifacts). Pruned-vocab caveat applies (50,256/128,256, ASCII-oriented) | ✅ | docs/hardware_logs/wikitext2_full_ppl_2026-07-17_newtokenizer_run.log |

## C. RANGER quantization research (Phase 1, real models)

| # | Claim | Verdict | Provenance |
|---|-------|---------|------------|
| C1 | E7 super-weight null RETRACTED as artifactual (absmax RTN reconstructs row max bit-exactly) | ❌→✅ | RESULTS-phase1.md; retracted-run JSON tracked b6885fd |
| C2 | Massive activations exist at 135M (332× max/median) — below literature's ~1B floor; input-independent | ✅ | E8, e8_activation_census_SmolLM2-135M.json |
| C3 | Weight-space census PREDICTS activation outlier channels (top-4 all in census top-6, SmolLM2) | ✅ | E8 bridge, RESULTS-phase1.md |
| C4 | Super-weight lever: NULL at 135M (holdout 24.870 vs baseline 24.896), REAL at 0.6B | ✅ | E9 + E12 + disjoint-slice confirmation 1cc6375 |
| C5 | Rotation is THE invariant: recovers 92.3% (135M) / 92.5% (0.6B) of A4 activation-quant collapse | ✅ | E10 + E12 |
| C6 | Census-channel static exemption: 59.1% recovery w/ proven specificity at 135M; collapses to ~0–25% on Qwen — architecture-DEPENDENT | ✅ | E11 + E12 |
| C7 | MSE clip sign-flips with scale: hurts 135M (+0.62), helps 0.6B (−2.40) — confirmed on disjoint slices | ✅ | confirmation pass 1cc6375 |
| C8 | A8 activation quant is FREE (16.78 vs 16.15 bf16); A4 without rotation collapses (13,773 PPL) | ✅ | E10 |

## D. Method / meta gains (how we work)

| # | Claim | Verdict | Provenance |
|---|-------|---------|------------|
| D1 | Adversarial multi-agent verification catches real errors (88-agent RFI audit → 77 remediated findings; 8-angle review caught B5 before it shipped) | ✅ | commits 8d6dcd4, c6ce416 |
| D2 | Anchor-pinned refactoring: byte-identical regression gates let the engine generalize without silent drift | ✅ | B2; tests/gemm_equivalence.rs pattern |
| D3 | Benchmark hygiene protocol (idle-CPU check, latched sensors, ≥240s windows) after the 8h runaway-ugrep poisoned a day of numbers | ✅ | measure_energy.sh guard; memory record |
| D4 | **NOVELTY KILL (2026-07-29 skeptic probes): "bare metal is the uniquely deterministic measurement platform" is MEASURED FALSE.** Hosted Linux `perf instructions:u` on the real engine at load 7.35 under crosvm: CV 0.000465%; best bare-metal prefill tick CV ever logged: 0.0780% — **168× in favour of the HOSTED arm**; third-party cloud-CI instruction-count noise ~1e-6% (Cachegrind/CodSpeed, external citation). "Bit-exact instruction count is structurally unavailable under an OS" also measured FALSE. Wall-time variance on the quiesced VM: CV 14.4% unpinned / 4.3% pinned (n=20 each). What bare metal remains uniquely for: firmware-level control (bd-PROCHOT), zero-OS TCB, boot time — NOT measurement determinism. | ❌ novelty claim killed | docs/hardware_logs/skeptic_variance_budget_kill_2026-07-29.log; skeptic_novelty_instret_kill_2026-07-29.log; skeptic_instruction_count_{kill,hosted_control}_2026-07-29.log |

## E. Corrections found by the 2026-07-16 audit workflow (wf_7bcb9985-6bc)

102 claims checked against primary sources: 91 CONFIRMED, 6 PLAUSIBLE, 5 REFUTED.

| # | Finding | Action |
|---|---------|--------|
| E1 | TECHNICAL_REPORT §2 row 4 says scalar fallback "4.01B cycles/token" — its own cited logs show 3.60–3.71B. Figure traces only to commit c9981f2's message. | fixed ✅ 2026-07-16 |
| E2 | B2/B3 above: Falcon-E coherence + anchor-reproduction were commit-message-attested, no logs | closed ✅ — both logged 2026-07-16, B2/B3 rows |
| E3 | RANGER E8 write-up detail "down_proj input argmax top channel covers 2–12% of tokens" understates: 8/30 layers exceed 12% (up to 98.5%); conclusion (token-dependent → no static exemption) UNCHANGED | fixed ✅ ranger commit 845fb3f |
| E4 | Theory-doc Pillar corrections were NOT applied in place (only §3.8 attribution was); pillar-status updates still pending in edge-quantization-hybrid.md | apply or ledger-close ⬜ |
| E5 | RANGER branches reconciled 2026-07-16: origin/main advanced by user-merged PRs #1/#2/#4 (cloud experiment labs A/C/D + QAT scaffold + CI, session_019yn6fEuV1GLreL7V9DMpsw = the interrupted workflow); local main fast-forwarded to 43121fd; phase1-real-model pushed through 845fb3f (push auth WORKS — old "blocked on gh auth" note is stale). ff6e651 (Experiment B KV-cache lab) merged as PR #5 per user instruction, local main at 64da6de. Remaining: unify cloud labs with local E7–E12 lineage; verify selftests CI green on main | mostly closed 🔶 |
| E6 | `scripts/phase0_meter_check.sh` hardcoded EXPECTED=12.80 (superseded anchor) AND its flagless invocation silently became chunked mode after the 750d377 merge — T0b was comparing chunked-vs-chunked (broken gate). The handoff's "flagless = sample pin" note describes the merge-losing evaluator | fixed ✅ 2026-07-16: script rewritten (--sample pin, EXPECTED=10.348, reference=test.txt), HANDOFF annotated |
| E8 | aegis-linux harness banner hardcodes "BitNet 1.58b 2B-4T" even when running Falcon-E (cosmetic; config-driven engine, stale banner) | fixed ✅ 2026-07-16: banner now prints layers/hidden/vocab/template from the artifact's config |
| E9 | inference.rs context overflow: decode loop bound was prompt+max_new_tokens with no window clamp → index panic at position 2048 (the 2026-07-14 energy-run crash); prompt clamp was hardcoded 1948 (BitNet-only assumption) | fixed ✅ 2026-07-16: window-derived bounds + regression test (fails on unfixed code, verified both directions) |
| E7 | RFI actual send is evidenced in Gmail (message id 19f628410e9786e1, verified in:sent 2026-07-14 per session record) but is NOT verifiable from local disk — keep the Gmail provenance in this row | recorded ✅ |

*Seeded 2026-07-16 from memory + commit history; audit workflow verdicts merged 2026-07-16 (run wf_7bcb9985-6bc: 14 agents, 302 tool calls, full output preserved in the workflow journal).*

## M. Model lab — train/distill/port research (workflow wf_914021b5-c1f, 2026-07-17, 28 agents, lens-diverse refuter panels)

| # | Claim | Verdict | Provenance |
|---|-------|---------|------------|
| M1 | Stock transformers AutoBitLinear offline-mode recipe on Falcon-E inverts the weight_scale convention → outputs ~300× wrong (ws² error); soft-prompt path SALVAGED only via ActQuant-STE patch (or inverting all 168 scales) + mandatory step-0 logit-parity gate. No prior art for prompt tuning over frozen ternary — we'd be first | ✅ measured on-box by refuter | program/model-lab/paths_full.json (soft-prompt votes); scratchpad test_bitlinear_equiv.py |
| M2 | Stock onebitllms cannot run on free-tier T4/P100: Triton ≥3.3 dropped Turing (CC 7.5). Env pin torch≤2.6 (triton 3.2) + onebitllms 0.0.4 + pinned TRL is the untested-but-only route; its export format IS byte-compatible with repack_ternary.py (source-verified: scale = 1/mean|w|, pack_weights, modules_to_not_convert=[lm_head]) | ✅ source-verified | paths_full.json (falcon-sft votes; raw quantization_utils.py fetched) |
| M3 | BitNet-2B-4T bf16 master (microsoft/bitnet-b1.58-2B-4T-bf16) declares quantization_mode "online" autobitlinear → plain transformers+trl trains it as QAT with NO Triton dependency (dodges the Turing problem); is_qat_trainable gates True; replication thin (one independent author) | ✅ mechanism / 🟡 replication | paths_full.json (bitnet-sft votes) |
| M4 | transformers CPU on this box: teacher-forced 6.4 tok/s @1.12GB RSS, but greedy decode 0.252 tok/s (refuter re-measured WITH log after catching an unlogged 0.34 assertion) → loglikelihood evals run LOCALLY (G4a ~10 min, tiny MC suite overnight); generation evals ONLY via the engine (4.8 tok/s) | ✅ | scratchpad falcon_e_smoke2.log + refuter re-measurement |
| M5 | Tiny ternary QAT training throughput MEASURED on this box (torch 2.13 CPU, 8t, B=8 T=512, absmean-STE + int8-act fake-quant, AdamW): 733 tok/s @8.5M params / 458 @21M / 138 @47M ⇒ 10–15M params trainable to TinyStories convergence (~300–500M tok) in 5–12 days local | ✅ measured | paths_full.json (scratch-tiny); raw f32 matmul ceiling 74.6–116.5 GFLOPS |
| M6 | "2× hidden width restores fp16 parity for ternary" REFUTED as parity claim — BitNet-Reloaded's own Table 1 contradicts it beyond the smallest cells; 2× width is a FLOOR. M7 target = quality of ≤half-size fp16 twin (twin must actually be trained — critic) | ❌→✅ negative finding | verify:scratch-tiny:replication vote; arXiv 2407.09527 Table 1 |
| M7 | TernaryLM (arXiv 2602.07374, Feb 2026) = 132M native-ternary TinyStories, val PPL 58.42, code public. Sub-50M ternary storyteller + bare-metal-no-OS boot = unclaimed first (absence-of-evidence caveat) | ✅ paper / 🟡 "first" | arXiv 2602.07374; searches logged in paths_full.json |
| M8 | Frontier-API-generated training data DEAD: Anthropic consumer ToS prohibit training on outputs + DARPA provenance risk. TinyStories itself is GPT-3.5/4-generated (CDLA-Sharing-1.0) and Magpie-Ultra is Llama-3.1-405B-generated (naming flow-down!) → sovereignty framing = "own training run on openly licensed data", ledger needs generator-model column | ✅ | critic.json gaps 1-2; dataset cards |
| M9 | Compute lanes: Kaggle primary (~30 GPU-h/wk P100/T4×2, TPU v5e-8, 12h background "Save & Run All", 200GB datasets); Colab free = interactive-only (ToS); SageMaker Studio Lab signups CLOSE 2026-07-30; TRC needs card-backed GCP (~$5–15/mo ancillary); HF ZeroGPU useless for training | ✅/🟡 quotas conflict, confirm on account | paths_full.json (compute votes) |
| M10 | Falcon-E-3B-Instruct: natively-ternary llama-family (32L, h2048, GQA 16/2, vocab 32768, untied, lm_head unquantized), main/bfloat16/prequantized revisions live, ~999MB packed, ~1.55GB est. runtime @4096 ctx — highest zero-training fleet-wide capability jump; TII superiority claim is vendor-single-source → must re-measure on our harness (M1 gate) | ✅ existence / 🟡 quality | paths_full.json (checkpoints votes; config.json fetched) |
| M11 | Local 8B-teacher bulk datagen DEAD (463 days/100M tok @2.5 tok/s); cross-tokenizer logit KD DEAD (32768 vs 128256); local full-FT of 1–2B PARKED (disk/RAM arithmetic) | ✅ arithmetic | paths_full.json (distill-data votes) |
| M12 | **tinybit round-trip gate PASS (2026-07-17): from-scratch ternary QAT trainer → export_hf (divide-convention weight_scale) → repack_ternary.py → engine; torch PPL 77.5159 vs engine 77.6730 = 0.20% (tol 3%), token parity 480/480.** 3.81M params, 250 steps, loss 8.38→4.16, 574s, peak RSS 1.88GB. M7 step-0 gate CLEARED — the own-training→bare-metal chain is proven at toy scale | ✅ | model-lab/tinybit/roundtrip_gate.log + gate_work/artifacts/ |
| M13 | **M1 Falcon-E-3B port MEASURED (2026-07-17/18): coherent structured chat (Rayleigh-scattering answer), engine PPL 4.393 (t2d 1896 tok) / 4.386 (WT2 sample 1898 tok) vs 1B same-mode 4.837 / 4.829 = ~9.2% better, same binary, same tokenizer — TII superiority independently replicated at PPL level on our harness; decode 2.5 tok/s contended. Remaining M1 sub-gate: RSS fit + boot on real 2GB pre-AVX2 iron (user hands) | ✅ engine-side | docs/hardware_logs/falcon_e_3b_bringup_2026-07-17.log |
| M14 | **M7a fp-twin lane COMPLETE (2026-07-18, wf_11dd4ba2-870 build+adversarial verify):** tinybit fp mode = single-variable swap (7 projections BitLinear→bias-free fp32 nn.Linear, identical init; RMSNorm/RoPE/GQA/SwiGLU/SubLN/tied head unchanged); configs runtime-counted m7_ternary **14,171,392** / m7a_twin **6,529,920** params (2.17×); twin 200-step smoke on real train_8k.bin PASS (loss first/last-10 mean 8.6115→4.3231, val PPL 192.2→114.8), SIGKILL-at-~106 + resume PASS (jump −0.12, optimizer+RNG restored); **negative finding: contended 8T 619.7 tok/s SLOWER than 4T 1174–1310 tok/s** (HT oversubscription + active eval); M7 ternary 10-step crash-test OK (472 tok/s sample). Verifier independently re-ran 30-step smoke + resume + guards and re-parsed every claimed number from raw logs (PASS_WITH_ISSUES; issues were evidence-logging gaps — guard refusal since live-logged). Guarded launcher + queue unit `m7a-twin-queue` armed: waits for aegis-ppl-rerun exit → 50-step 4T-vs-8T re-bench on idle box → launches 500,002,816-token twin run at winner thread count | ✅ smoked, run queued | model-lab/tinybit/logs/m7a_twin_smoke_2026-07-18.log, m7_param_counts_2026-07-18.log, m7a_fp_mode_unitchecks_2026-07-18.log, m7a_twin_queue_2026-07-18.log |
| M15 | **M3 MC harness BUILT (2026-07-18, same workflow):** aegis-eval `--mc` mode (ids-based, lm-eval continuation convention; per-choice teacher-forced NLL over continuation positions by differencing two calculate_perplexity runs — aegis-core untouched; acc=argmin ΣNLL, acc_norm=argmax −NLL/utf8_byte_len, ties→lower); ARC-Easy val n=100 seed 42 prepped (scoped 86,080-B parquet, sha256 in NOTE.txt, CC BY-SA 4.0; tokenizer parity vs G4a ref tokens 2410/2410, no-BOS convention); 3-item identical-ids smoke: engine-vs-transformers predictions 3/3 identical, max \|ΔNLL\| 1.16 nats vs bf16 batch-noise floor 0.20; cargo fmt/clippy −D warnings/integrity_check all pass; verifier re-encoded all 100 ctx + 399 continuations (0 mismatches) and reproduced builder reference bitwise. **n=100 parity gate PASS (2026-07-18 09:15Z, closed by systemd unit m3-gate-close):** acc engine 0.6900 vs ref 0.6500 → \|Δ\| 4.00 pts; acc_norm 0.6300 vs 0.6200 → \|Δ\| 1.00 pt (both inside ±5 gate); per-choice NLL over 399 values: mean abs diff 0.2367 nats (≈ the measured bf16 batch-noise floor 0.202), max abs 1.81 nats, max rel 15.0%; pred disagreements 5/100 raw, 4/100 norm (near-tie items). Engine MC numbers are now trustworthy for baselines; tighter parity would need an fp32 reference (optional) | ✅ PASS |
| M16 | **M3 baselines, ARC-Easy engine-side (2026-07-18): Falcon-E-3B shows NO measurable advantage over 1B at n=100 despite its ~9.2% PPL edge** — 1B acc 0.6900/acc_norm 0.6300 vs 3B 0.6600/0.6200, same 100 items; paired: both-correct 60, only-1B 9, only-3B 6, McNemar exact p=0.61. Honest reading: PPL gains did not translate to MC gains at this resolution. **RESOLVED at n=570 (full validation split, same day): 1B 0.6965/0.6193 vs 3B 0.7193/0.6316 — 3B trend +2.28 raw / +1.23 norm pts, McNemar exact p=0.2414 / 0.5692 → still NOT significant.** Discordants raw: only-1B 46, only-3B 59. Detecting +2.3 pts at this discordant rate needs n~2000+. Final reading: 3B is never worse and trends better, but its ~9.2% PPL edge buys no *provable* ARC-Easy gain; flagship-base choice must weigh size (953 vs 635 MB) + speed (2.57 vs ~4.8 tok/s) against an unproven +2 pts; M6 domain eval is the real decider. 3B MC throughput 2.57 tok/s single-thread contended (15,277 tok / 5,941 s) | ✅ resolved: trend +, not significant at n=570 | docs/hardware_logs/m3_mc_baseline_falcon_e_3b_2026-07-18.log (incl. paired analysis), m3_mc_full570_falcon_e_{1b,3b}_2026-07-18.log | docs/hardware_logs/m3_mc_parity_arc_easy_2026-07-18.log, model-lab/data/evals/arc_easy/ (incl. smoke3_evidence/) |
| M17 | **M5b GATE PASSED (2026-07-20): BitNet-2B-4T online-QAT SFT is VIABLE on free Kaggle 2×T4** — kernel aefinityaiinc/m5b-bitnet-qat-smoke v6: fp32 weights + fp16 AMP seq 1024 lr 1e-4 paged_adamw_8bit grad-ckpt device_map=auto FITS (first-choice config, no fallback); 150 steps smoltalk slice, loss head5 0.9350 → tail5 0.7371, zero NaN. Recipe corrections vs plan: transformers ≥5.x NATIVE BitNet (bf16 master repo advertises nonexistent remote code — trust_remote_code OFF), plain transformers Trainer (trl 1.8 incompatible with BitNet 3 ways), tokenizer needs pad_token=eos. 6 iterations to green, all documented | ✅ M6 vehicle candidate confirmed | docs/hardware_logs/m5b_smoke_result_2026-07-19.json (loss curve), m5b_smoke_kernel_stdout_2026-07-19.log, m5_smoke_iterations_2026-07-19.log |
| M18 | **NEW KILL (2026-07-19/20): onebitllms is unusable on ALL free Kaggle GPUs even in the pinned env** — with torch 2.6 + triton 3.2 correctly targeting sm_75, its BitLinear Triton kernels HARDCODE bf16 ops: `ptxas: Feature '.bf16' requires .target sm_80 or higher`. T4=sm_75, P100=sm_60 → no free Kaggle GPU has bf16 silicon; the earlier "pin Triton ≤3.2" kill was necessary but NOT sufficient. In-place bf16→fp16 kernel patch (v5) FAILED NUMERICALLY: pretrained-model SFT started at loss 9.68 (random-init level — fp16 quant path destroys weights at step 0) then collapsed to EXACTLY 0.0 for 95 steps; the kernel's printed "PASS" was a false positive of the naive tail<head criterion, caught on review. bf16 silicon is ESSENTIAL to onebitllms. Falcon-E QAT SFT on free tier needs a pure-PyTorch STE BitLinear reimpl (untested) or sm_80+ hardware. **M6 vehicle = BitNet path (M17) by elimination** | ❌ KILL confirmed | docs/hardware_logs/m5_smoke_iterations_2026-07-19.log |
| M19 | **M2 corpus census COMPLETE (2026-07-21): 33 subsets enumerated from local parquets** — smoltalk ≈1,034M Falcon-tok (13 subsets + union config), tulu-3-sft-mixture ≈940M (19 sources); rows exact, token totals sampled estimates (400/subset seed 42, honest-labeled). Notables: smol-magpie-ultra 667M F (Llama-naming flow-down decision), wildchat 330M (GPT-4-generated, flag), no_robots CC-BY-NC DROPPED, clean KEEP pool ≈190M F ≈ 50× M6 replay need. Full table PROVENANCE.md; VERIFY-CARD rows resolved only at mix freeze | ✅ | model-lab/data/m2_corpus_stats.{json,log} (unit m2-corpus-stats, 234s) |
| M20 | **M7a fp32 TWIN RUN COMPLETE (2026-07-22 ~01:44Z): 500,002,816 tokens / 122,071 steps in 65.82h @ 4T (avg 2,110 tok/s), FINAL VAL PPL 5.513** (series min 5.447@116k; peak-LR plateau 7.8–8.7 then monotone cosine-decay improvement — the no-mid-run-surgery call held). Final train loss ~1.35–1.49. Checkpoint m7a_twin.pt archived to BOTH external drives. This is the honest fp32 baseline the M7 ternary (14.17M params, 2× param handicap per BitNet-Reloaded floor-not-parity finding) must approach. **M7 ternary launched 2026-07-22 02:0xZ, unit m7-ternary, same data/budget/threads/schedule, one variable changed (linear=bitlinear)** | ✅ twin baseline pinned | docs/hardware_logs/m7a_twin_final_2026-07-21.log; model-lab/tinybit/logs/m7a_twin_full_2026-07-18.log + m7_ternary_full_2026-07-21.log |
| M21 | **M7 SOVEREIGNTY VERDICT (2026-07-28): ternary BEAT the fp32 twin — final val PPL 5.140 vs 5.513 (6.8% better) at identical 500M-token budget/data/order** (142.98h @4T, 971 tok/s avg). DISCLOSURE: recipe-vs-recipe comparison (twin cosine+wd, ternary BitNet two-stage), NOT single-variable ablation; param handicap 2.17× per BitNet-Reloaded floor finding; deployment bytes ternary ~3.5MB vs twin ~13MB fp16. **FINAL-ARTIFACT ROUND-TRIP GATE PASS: torch 3.6808 vs engine 3.6880 = 0.19% (tol 3%), 471/471 token parity** — after catching a wrong-tokenizer setup (4k vs 8k) that produced garbage PPLs on both sides, itself evidence the parity guard works. The from-scratch sovereignty model is trained, verified, and engine-ready; remaining: coherence spot-check + boot on real 2GB iron (user hands) | ⚠️ HEADLINE DEMOTED BY M24 (Band 2 cooldown-major, 2026-07-29) / ✅ artifact verified | docs/hardware_logs/m7_ternary_final_2026-07-27.log, m7_final_roundtrip_2026-07-27.log; artifacts archived both drives |
| M22 | **M7 coherence spot-check PASS (2026-07-28): the 2.8MB sovereignty model generates coherent TinyStories-class English on the engine** — 3/3 prompts produced grammatical, narratively consistent continuations (tracked pronouns, formatted dialogue); **decode ~470–507 tok/s single run on aegis-linux** (vs 4.8 tok/s Falcon-1B / 8.25 BitNet-2B multicore — the small-fast end of the portfolio dial is real). M7 lane complete except real-iron boot (user hands) | ✅ | docs/hardware_logs/m7_coherence_spotcheck_2026-07-28.log |
| M23 | **A4/M9a TPU gate attempt (2026-07-28): Kaggle SILENTLY DOWNGRADES Tpu1VmV38 requests to CPU workers** — v2 forensics: zero TPU env vars, no /dev/accel*, no vfio; jax sees CpuDevice only (both attempts, ~6s runs). GPU requests honor machine_shape on the same account, so this is TPU-pool availability/gating, not auth. Action: periodic retry (availability fluctuates); A4 not blocked near-term (M6 is the active cloud lane) | 🟡 infra finding; retry later | ~/model-lab/kaggle/a4-tpu-smoke/ + kernel output logs |
| M24 | **M7 LR-COOLDOWN CONFOUND ABLATION COMPLETE (2026-07-29): BAND 2 — COOLDOWN-MAJOR. The M21 "ternary beat the fp32 twin" headline is UNSUPPORTABLE AS STATED.** Preregistered before the contrast existed (git 1ee1f5c). 512 disjoint windows, paired sign-flip + bootstrap, Holm-corrected, all arms scored in one run: d_gap (ternary vs twin) **+0.073351 nats/tok** [CI +0.070252..+0.076525, 500/512]; d_cool (hold vs cooled twin) **+0.039480** [CI +0.037635..+0.041389, 495/512]; d_tokens **+0.003684** (passes the 0.30·d_gap validity check — the extra 16.38M tokens did little); **R = d_cool/d_gap ≈ 0.54, A LOWER BOUND** (arm K annealed 3.3% of training; the ternary annealed 25%). Residual ternary-vs-cooled-twin **+0.030187 nats** [+0.027539..+0.032900, 427/512, Holm p 5.00e-05] is significant but NOT attributable to ternary: the ternary arm carries **2.17× the parameters** (14,171,392 vs 6,529,920). Per the prereg bands: report the decomposition; do not re-report the residual as a ternary win. Supersedes M21's headline (M21's own DISCLOSURE line flagged recipe-vs-recipe; M20's "one variable changed" was refuted — seven knobs differ). | ⚠️ M21 headline demoted; decomposition stands | docs/hardware_logs/m7lr_PREREGISTRATION_2026-07-29.md; m7lr_armh_verdict_2026-07-29.log; m7lr_verdict_twin{,_hold,_cooled}_2026-07-29.json; m7_paired_eval_baseline_2026-07-29.log |
| M25 | **GATE 2 (2026-07-29): the DEPLOYABLE retargeted ternary artifact round-trips — torch 5.5853 vs engine 5.6390 = 0.95% (tol 3%), 471/471 token parity** (instrument: RAW_m7_retarget_roundtrip_gate.log). On-device mission retargeting is real — and fidelity degrades under it: 0.19% → 0.95% (5×), still inside tolerance but trending wrong; watch before chaining retargets. Chat-domain PPL start 607.2116 (instrument: flip-rate log). ⚠️ Prose-attested (commit 3162a2c only, instrument capture PENDING — do not use externally): 614,400 smoltalk tokens / constant lr 1e-4 / ~10 min CPU / 1,199 MB RSS; domain end-PPL 72.44; ternary forgetting 3.3× the fp twin's (−0.6121 vs −0.1853 nats); constant-beats-anneal 0.4168 nats. Retargeted model answers in chat register but is incoherent (spot-check 2026-07-30) — 614K tokens moved style, not competence. | ✅ round-trip instrumented / ⚠️ recipe+forgetting prose-attested | docs/hardware_logs/m7_retarget_gate2_evidence_2026-07-30/ (RAW log + frontier/twin JSONs + md5s); commit 3162a2c; model-lab/tinybit/m7_retarget_gate_work/ |
| M26 | **TERNARY RETRAINABILITY DECOMPOSED (2026-07-29, L03 flip probe + freeze control): the ternary core does not need to move — freezing all 11,010,048 ternary weights and training only the fp periphery (embeddings/norms/head) captures ~70% of the domain gain at ~5% of the forgetting.** freeze=ternary: domain +1.5834 nats [CI +1.3814..+1.7868, 48/48], general −0.0148 [−0.0178..−0.0120] with **0 flips**; freeze=none: domain +2.2722, general **−0.2731** (18× more forgetting); freeze=fp inverse control: domain +1.3800, general −0.2520 → the forgetting lives in the ternary flips; most of the domain gain does not. Flip rates scale with LR: mean/step 3.84e-4 @1e-5, 1.17e-3 @1e-4, ~1.16e-2 @1e-3 (net 11.6%/100 steps, general −1.7005 nats = catastrophic). Fragility surface of m7_ternary.pt: 20.072% of latents within 10% of a ternary boundary; 36.637% code==0. Literature context: only one published flip-rate number exists (arXiv 2412.04787, ~0.05%/step) — this instrumentation is largely unpublished territory. | ✅ measured | docs/hardware_logs/m7_ternary_flip_rate_L03_2026-07-29.log; model-lab/tinybit/probes/flip/*.json |
| M27 | **LM-head 2-plane certified-screen probe (2026-07-29): deployed int16 fixed-point table = 0/12288 argmax flips vs BF16 (max logit Δ 0.003173); HI-plane screen provably keeps argmax, survivors p50 2 / p90 7 / p99 15 / max 43 → lm-head stream 6,291,456 → ~3,146,496 B/step (2.000× less) at ≤0.52% extra MAC.** Candidate engine optimization, NOT implemented; probed at M7 shape only (V=8192 d=384). | ✅ probe / ⬜ engine impl | docs/hardware_logs/m7_lmhead_argmax_probe_2026-07-29.log |
