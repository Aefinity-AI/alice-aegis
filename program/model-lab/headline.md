Ship the zero-training wins immediately (Falcon-E-3B port, G4a eval gate, corpus downloads), gate every training lane on an hours-scale smoke test, and split the training work by venue: the Chromebook trains the sub-50M ternary sovereignty model at measured throughput while free Kaggle GPU carries the 1-2B instruct specialization. The refuter panels killed all three "stock tooling just works" assumptions — soft-prompt's scale convention (outputs wrong by ~300x), stock onebitllms on free T4 (Triton dropped Turing), and tiny-ternary fp16 parity (2x width is a floor, not parity) — so the M-series front-loads cheap parity gates (M0 tonight, M4a logit match, M5 smokes) before any multi-day commitment. If gates pass, all four required deliverables land at $0 in roughly 4-6 weeks of mostly-unattended wall-clock; the only spend decision is the optional ~$5-15/mo TRC ancillary-cost exception, which is the user's call.