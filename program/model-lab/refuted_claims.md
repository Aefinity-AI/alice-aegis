- **soft-prompt (KILLED 2/3, salvaged in weaker form — adopted as M4)**: "Stock transformers AutoBitLinear(online_quant=False) computes the exact ternary forward for Falcon-E, so soft prompts train locally with no changes" — REFUTED by execution on the target box. BitLinear divides by weight_scale, AutoBitLinear multiplies; the Falcon-E checkpoint stores divide-convention scales (8.6-37.5), so the stock recipe's outputs are wrong by weight_scale^2 (~301.9x measured on real layer-0 q_proj). Additional kills: HF's quantizer flags offline mode is_trainable=False (custom loop required); @torch.compile crashes on this VM without python3-dev (TORCHDYNAMO_DISABLE=1 mandatory); zero independent replication of this path exists anywhere. Salvaged version: BitLinear + ActQuant-STE patch (or invert all 168 scales) + mandatory step-0 logit-parity gate before any multi-day run.
- **falcon-sft (1/3 refuted — correction is binding)**: "Stock onebitllms runs on free Kaggle/Colab T4 in 2-5 hours" — REFUTED. Triton >=3.3 (bundled with torch >=2.7) requires NVIDIA CC 8.0+ and dropped Turing; T4 is sm_75; the researcher's own cited source says so. The path survives only behind an UNVERIFIED torch<=2.6/triton-3.2/pinned-TRL environment (the M5c gate), stacked on the already-flagged unproven bf16-on-Turing numerics. Also corrected: "8xA10G ~8.5h" appears in no cited source (drop it); single-T4 fp32+Adafactor is razor-thin (~14.9GB vs ~15GB usable) — T4x2 with ZeRO is the robust free config.
- **scratch-tiny (1/3 refuted — quality framing corrected)**: "2x hidden width buys fp16 parity for ternary QAT across 100K-48M params" — REFUTED on replication. Single group (Nielsen & Schneider-Kamp), unreplicated, and the paper's own Table 1 shows ~25% PPL gap at its 48M cell despite 2x width; the "surpasses" phrasing belongs to the paper's vision models. Independent evidence cuts against parity: TernaryLM 132M shows +13.8% matched-size PPL tax; Spectra shows parity only >1B. Corrected planning basis (used in M7): 2x width is a FLOOR; budget quality against a <=half-size fp16 twin. Feasibility (measured local throughput) fully survives.
- **checkpoints (0/2 refuted — superiority claim scoped down)**: "Falcon-E-3B beats BitNet-2B on TII's own published benchmark table" — corrected: the 22.65-vs-17.70 numbers come from two SEPARATE tables (BitNet is not in the 3B table); TII's only direct head-to-head (Leaderboard v1) is 53.17 vs 51.54 with BitNet winning GSM8K, HellaSwag, and TruthfulQA. All superiority evidence is a competitor's self-reported eval — the same vendor-blog single-sourcing that burned this program before. Guardrail: no DARPA citation until re-measured on the program harness (M1 gate). Minor: the Bonsai-27B "hybrid attention" aside is cited to a URL that doesn't support it (conclusion conservative, still out of scope); Falcon-E "1B" is really 1.8B params — never copy HF's packed-repo param counters into grant docs.
- **evals (0/1 refuted — provenance defect corrected)**: the 0.34 tok/s transformers decode figure existed in NO log — asserted as measured, but wasn't. Verifier re-measured 0.25 tok/s under identical conditions; conclusion strengthened (transformers-side generation evals ~240h, even deader). Also: "~50 GFLOPS effective" was arithmetic-wrong (actual ~23); harmless since all times are directly measured. Guardrail: measurement doctrine means every number needs a log file, including our own.
- **distill-data (0/2 refuted — license corrections binding for DARPA)**: "SmolTalk is Apache-2.0" — only the four NEW synthetic subsets are; the repo carries no overall license tag and the OpenHermes-2.5 slice declares none. Per-subset license ledger is mandatory (M2). "Best published open SFT data" — superseded by smoltalk2 (also free; strengthens the conclusion). "LIMA/LIMIT shown across 1B-13B" — actually 7B-65B; the at-1B extrapolation is plausible but uncited.
- **compute (0/1 refuted — stale facts corrected)**: "HF ZeroGPU = 3.5 min/day on H200" — current docs say RTX Pro 6000 Blackwell, 5 min/day free (conclusion unchanged: useless). "T4 16GB typical" is not in the Colab FAQ cited. Modal's "no card required" is not stated on its pricing page (verify at signup). And the tasking's own premise "~38GB free, no swap" is stale — measured 2026-07-17: 6GB swapfile active, ~30GB free; re-measure disk before any large download.
- **bitnet-sft (0/3 refuted — precision corrections)**: "21 HF derivatives of the bf16 master" — confirmed at least 7 (1 finetune + 6 adapters) plus merges; exact count unconfirmed. Kaggle 2xT4 full-FT is "tight (~14.5GB/GPU)", not "comfortable" — plan 8-bit Adam + grad checkpointing + optimizer CPU offload fallback. The CUDA-only and ~20%-QAT-overhead figures come from the onebitllms README, not the Axolotl docs. Replication note: exactly one independent author (jpacifico/Aramis) has run this path, self-reported; the one documented failure used lr 5e-7 — LR ~1e-4 is load-bearing.
- **Standing kills from this cycle and before (on the record, not relitigated)**: PTQ ternarization of dense sub-7B checkpoints collapses quality (settled 2026-07-12; also bars Falcon3-*-1.58bit ports); free-tier ternary recovery refuted 0-3; local 8B-teacher bulk data generation dead by arithmetic (463 days per 100M tokens, weak teacher); Claude-as-training-data-teacher dead by Anthropic Consumer Terms + DARPA provenance (Claude remains tooling/eval-only); HF1BitLLM's image-only benchmark table stays UNVERIFIED (its 12.2 PPL claim was previously refuted 0-3) — anchor no ladder tier on it.