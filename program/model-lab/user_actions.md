- **Sign up for AWS SageMaker Studio Lab TODAY** — new signups close 2026-07-30 (13 days out) and approval has historically taken days. Free T4 lane with no card and no AWS account; grandfathering in costs nothing even if never used. Only you can create accounts.
- **Phone-verify the Kaggle account** — required for GPU access and internet-enabled notebooks; needed before the M5b/M5c smokes this week. While logged in, confirm the actual GPU (~30 h/wk) and TPU quota figures on your own account page — sources conflict and the schedule is built on them.
- **Apply to TRC now and make the credit-card call** — the application takes minutes and decisions take days. TPU time is free, but GCP billing verification requires a card and ~$5-15/mo of ancillary VM/GCS charges, which technically breaks the zero-budget rule. Your explicit decision; only needed for M9 scale-ups, so applying now loses nothing.
- **Fix ChromeOS power settings before M7** — chrome://settings/power: disable sleep while charging and lid-closed suspend (or keep lid open / external display), stay on AC. Multi-day local runs die on VM suspend otherwise.
- **Rotate the exposed HF token** (already a send-gate in the RFI package state) and provision a write-scoped token for Kaggle-to-HF checkpoint pushes — the 12h-session resume discipline in M6 depends on it. Only you can access HF account settings.
- **Plug in the external drive for mirroring** — Falcon-E-3B packed + bfloat16/prequantized masters (~7GB), and any Bonsai artifacts. prism-ml is a startup and HF1BitLLM a demo org; checkpoint availability is not permanent.
- **License flow-down review** — TII Falcon-LLM License (Apache-derived + acceptable-use policy) for any Falcon-E derivative entering the DARPA package, and Meta Llama 3 terms (redistribution/branding) for the M8 port. A judgment call only you or counsel can sign off.
- **Optional: register Modal** as the burst lane ($30/mo recurring credits, ~50 T4-h). The "no credit card required" claim is unverified — abort signup if a card is demanded.
- **Colab discipline** — use free Colab interactively only; do not approve any keep-alive/auto-reconnect automation. Google's FAQ says free-tier non-interactive use gets terminated, and the Google account is load-bearing for the program.